# AgentDeck P0 deterministic baseline

## Scope and authority

This record closes the documentation-only P0 architecture-reset baseline. It is
not a release claim, a V1 implementation claim, an adapter-readiness claim, or
Golden Mission evidence.

- Baseline plan start: `f396872078d2659b6bc2502f91c74045bae043c7`
- Baseline implementation end: `353b6aa7662b5ae06f3657244afc384e58200ca3`
- Observed at: `2026-07-18T11:33:00+08:00`
- Environment: conda `agentdeck`
- Python: `3.12.13`

The filename remains dated `2026-07-17` because this evidence belongs to that
P0 milestone; the observed timestamp records the actual execution date.

## Environment and safety gates

Every verification command explicitly removed the environment names below
without reading or recording their values. This shell array is the literal,
ordered, value-free `env -u` prefix used by the recorded commands:

```bash
SAFE_ENV=(
  env
  -u AGENTDECK_ACP_LIVE
  -u AGENTDECK_ACP_COMMAND
  -u AGENTDECK_M2C_LIVE
  -u AGENTDECK_M2C_STRICT_PREFLIGHT
  -u AGENTDECK_M2C_CODEX
  -u AGENTDECK_M2C_CLAUDE
  -u AGENTDECK_M2C_CLAUDE_ACP
  -u AGENTDECK_M2C_CLAUDE_ACP_PACKAGE
  -u AGENTDECK_M2C_NODE
  -u AGENTDECK_M2C_TMUX
  -u AGENTDECK_M2C_LEADER_MODEL
  -u AGENTDECK_M2C_AUTHORITY_DIGEST
  -u AGENTDECK_M2C_PROBE_SCOPE
  -u OPENAI_API_KEY
  -u ANTHROPIC_API_KEY
  -u DEEPSEEK_API_KEY
  -u OPENROUTER_API_KEY
  -u GEMINI_API_KEY
)
```

Expanding `"${SAFE_ENV[@]}"` produces `env` followed by every `-u NAME` token in
the exact order executed. No real provider, ACP, tmux, preflight, or live
acceptance node ran. The focused daemon tests exercised deterministic/local
fake paths: the tests use `FakeLeaderProvider`, the repository fake ACP agent
fixture, and test-created fake `tmux` executables. They did not establish real
adapter or terminal readiness.

## Scope audit

The following checks ran before verification:

```text
git merge-base --is-ancestor f3968720 HEAD
git diff --name-only f3968720..HEAD -- src/agentdeck tests
git diff --name-only HEAD~7..HEAD -- src/agentdeck tests
```

- The ancestor check exited `0`.
- Both source/test diff checks produced no output.
- Therefore the recorded P0 range changed neither `src/agentdeck` nor `tests`.
- Before this record was created, `git status --short` produced no output.

## Verification results

All commands ran serially in conda environment `agentdeck` with the safety
gates above removed.

```bash
# Python version: /usr/bin/time wraps the value-free env and conda command.
/usr/bin/time -p "${SAFE_ENV[@]}" \
  conda run --no-capture-output -n agentdeck python --version

# Compile: the same wrapper ordering as the Python version command.
/usr/bin/time -p "${SAFE_ENV[@]}" \
  conda run --no-capture-output -n agentdeck \
  python -m compileall -q src tests

# Focused suite: env sets PYTHONPATH, then /usr/bin/time wraps conda/pytest.
"${SAFE_ENV[@]}" PYTHONPATH="$PWD/src" /usr/bin/time -p \
  conda run --no-capture-output -n agentdeck pytest \
  tests/test_conversation_acceptance.py \
  tests/test_daemon_acceptance.py \
  tests/test_daemon_crash_matrix.py \
  tests/test_mission.py \
  tests/test_mission_orchestration.py \
  -q -rs

# Default full suite: the same env/time/conda ordering as the focused suite.
tmp=$(mktemp /tmp/agentdeck-p0-full.XXXXXX)
"${SAFE_ENV[@]}" PYTHONPATH="$PWD/src" /usr/bin/time -p \
  conda run --no-capture-output -n agentdeck pytest -q -rs \
  >"$tmp" 2>&1
```

The full-suite temporary capture was inspected for the summary and skip
reasons, then removed with `rm "$tmp"`; it was never written into the
repository.

| Check | Exit | Result | Pytest elapsed | Wall `real` |
| --- | ---: | --- | ---: | ---: |
| Python | 0 | `Python 3.12.13` | n/a | 0.60s |
| Compile | 0 | no diagnostics | n/a | 0.74s |
| Focused | 0 | 304 passed, 0 failed, 0 skipped | 33.81s | 34.66s |
| Default full | 0 | 4461 passed, 0 failed, 3 skipped | 227.70s | 228.68s |

`Pytest elapsed` is pytest's own reported duration. `Wall real` is the `real`
value emitted by `/usr/bin/time -p` around the command in the position shown
above; the two columns intentionally measure different boundaries.

## Skip inventory

The default full suite reported exactly three skipped nodes and three unique
reasons:

| Node | Count | Exact reason |
| --- | ---: | --- |
| `tests/test_acp_runtime.py:550` | 1 | `explicit opt-in required: set AGENTDECK_ACP_LIVE=1` |
| `tests/test_m2c_live_acceptance.py:7880` | 1 | `requires separately authorized explicit M2c authority preflight` |
| `tests/test_m2c_live_acceptance.py:12440` | 1 | `set AGENTDECK_M2C_LIVE=1 for real M2c acceptance` |

These skips are the evidence that the opt-in ACP, strict preflight, and real M2c
acceptance nodes did not run. No skipped node was reclassified as passing.

## Final classification

**Deterministic P0 baseline: PASS.** Compilation, the focused deterministic
suite, and the default full suite exited successfully with the exact counts
above. This classification does not imply V1 readiness, real Codex/Claude
adapter readiness, ACP/tmux readiness, Golden A/B readiness, or release
readiness.

The historical M2c runs remain legacy evidence. This baseline neither marks
M2c PASS nor authorizes or attempts another M2c preflight/live retry.

The documentation commit is required to contain only this file and
`HISTORY.md`; the post-commit verification must confirm `git status --short`
has no output.
