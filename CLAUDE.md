# CLAUDE.md

本文件帮助 Claude Code 或其他 coding agent 快速理解本项目。

## 项目定位

AgentDeck 是一个 local-first 多智能体终端工作台。目标是用任意可通过 API 调用的 LLM 做 Leader Agent，调度多个 Worker Agent，在 tmux 可见终端里执行任务，并通过消息账本、审批、状态存储和 ProjectView 保持可审计、可恢复。DeepSeek 可以作为首个默认 provider，但不是架构绑定点。

2026-07-11 自然语言 Mission Phase 0 已通过全新项目真实验收：两条用户消息驱动 Codex/Claude 完成 8 步冻结顺序，证据见 `docs/validation/2026-07-11-natural-language-mission-acceptance.md`。首次目录 trust 仍是 human setup，不得由 Worker 任务输入或静默 Enter 绕过。

Protocol-native Phase 1 已实现协议 records、append-only lineage、compact ProjectView、`protocol-runtime/v1` contract、只读 `agentdeck protocol status` 和 runtime capability metadata。Phase 2 foreground ACP v1 vertical slice 已完成 fake-Agent conformance，并于 2026-07-12 通过真实 `@agentclientprotocol/claude-agent-acp@0.58.1` 验收；证据见 `docs/validation/phase2-claude-agent-acp-vertical-slice.md`。Phase 3 M1 已交付裸 `agentdeck` 持续自然语言会话、显式 Leader/Worker transport 与精确 Mission preview/confirm；M2 已交付单项目 authoritative daemon，使确认后的 frozen Mission 可在客户端断开后继续，通过 AgentDeck 按顺序调度 ACP/tmux Worker、持久化 compact handoff、恢复、权限、接管和重连。证据见 `docs/validation/2026-07-13-phase3-m1-foreground-conversation.md` 与 `docs/validation/2026-07-13-phase3-m2-project-daemon.md`。`protocol status` 与 ACP preflight 必须保持只读：不得写 state/event、调用 provider、读取或输入 tmux、改变 permission、启动 adapter 或认证。能力元数据只描述 transport 能力，不是授权；tmux 只能标为 observable fallback，绝不得标成 ACP compatible，也不得与 ACP 互相静默 fallback。A2A、remote daemon、global roaming、Workspace Client、自动安装/认证和完整 transcript 恢复仍未实现。

Skill 是北极星的一部分：AgentDeck 后续应支持内置 skill、项目本地 skill 和显式 allowlist 的外源 skill，但 skill 必须通过 Skill Registry 加载并记录 path/source、hash、content snapshot、调用者和用途；skill 是可审计工作流上下文，不是绕过 approval、runtime safety 或 tool 权限的后门。

北极星 Skill 诉求：AgentDeck 应该逐步形成可内置、可外源、可建议、可审阅、可加载、可回放的 skill 生态。内置 skill 用来沉淀稳定高频工作流；外源 skill 可以来自用户、本地目录或未来 marketplace，但必须先经过只读 preview、hash/provenance 展示、显式 import、显式 load 和审计。任何 agent 都不得把外源 `SKILL.md` 静默安装、自动启用、当作权限授权，或绕过 human approval。

Skill Registry MVP：`agentdeck skills list` 必须只读发现内置 skill 和 `.agentdeck/skills/<name>/SKILL.md` 项目本地 skill，并暴露列表级 import control 和每个 skill 的 show/load controls；`agentdeck skills import-preview --path <SKILL.md>` 必须只读解析外源 skill，返回 source、目标项目路径、hash、覆盖状态、只读 allowlist 状态（`source_allowlisted`/`enforcement_active`/`import_blocked`）和 import/force/show controls，不复制文件、不追加事件、不 load、不阻断；`agentdeck skills import --path <SKILL.md>` 必须显式把外部 skill 复制进项目 `.agentdeck/skills/<name>/SKILL.md` 并追加 `skill_imported` 审计事件，默认拒绝覆盖，只有 `--force` 可覆盖；import 必须对 `[skills] allowed_sources` 做 opt-in 强制：allowlist 为空时不强制（向后兼容，行为不变），allowlist 非空且 source 不在任一 allowed source 下时必须拒绝导入（不复制、不写事件、返回非 0，stderr 提示加入 allowlist 或加 `--allow-unlisted`），只有显式 `--allow-unlisted` 逃生阀可越过；两条路径都通过 `skill_imported.allowlisted` / `.allow_unlisted` 审计；catalog/sources/import-preview 保持只读非阻断，只有 `skills import` 强制；`agentdeck skills show --name <name>` 必须只读返回 skill metadata、hash、content 和同源 controls；`agentdeck skills load-preview --name <name> --agent <id> --purpose <text>` 必须只读返回目标 agent、purpose、skill summary、显式 load command、show/load controls，以及只读依赖提示 `unmet_dependencies`（该 skill 声明的 `depends_on` 中未出现在已发现 skill 里的名字，复用 `resolve_skill_dependencies`）和 `has_dependency_cycle`（bool）；该依赖提示只是信息展示，不得阻断 preview、不得 auto-load/auto-import 任何依赖，也不写 `skill_loads[]`、不追加 `skill_loaded`；`agentdeck skills load --name <name> --agent <id> --purpose <text>` 才能写入 `skill_loads[]` 和 `skill_loaded` 审计事件，并保存 content snapshot；`agentdeck skills suggest --name <name> --summary <summary> --rationale <rationale> --source <source>` 只能写入 pending `skill_suggestions[]` 和 `skill_suggested` 审计事件；`agentdeck skills suggestions` 只能只读列出 suggestion queue，并为 pending item 派生 draft-preview control；`agentdeck skills draft-preview --suggestion-id <id>` 只能只读生成拟写入 `SKILL.md` 的内容、hash、目标路径和显式 create control，不写文件、不更新 suggestion status；`agentdeck leader chat --message "创建 skill 建议 sgs_xxx"` 只能进入只读 `mode=skill_create_preview`，嵌入 `skill_create_preview_card` 并返回显式 `agentdeck skills create --suggestion-id <id> --confirm`，不得创建文件或修改 suggestion queue；`agentdeck skills create --suggestion-id <id> --confirm` 是 suggestion 草稿落地为项目 skill 的唯一路径，必须要求 `--confirm`，写入 `SKILL.md`、标记 suggestion 为 `created` 并追加 `skill_created`，但不得自动 load、调用 provider、读取 tmux 或修改 approval/runtime state。skills 命令不得调用 provider、读取 tmux、发送输入、修改 approval/runtime state，外源 skill 不得静默安装或启用，suggestion 不得绕过 create 确认直接生成 `SKILL.md`，import/create 后仍必须显式 load 才能进入 Leader/Worker 上下文。

Memory Suggestion MVP：`agentdeck memory suggest --summary <summary> --rationale <rationale> --source <source>` 只能写入 pending `memory_suggestions[]` 和 `memory_suggested` 审计事件；`agentdeck memory suggestions` 只能只读列出 queue，并为每条 pending item 派生 `apply_preview` / `apply_memory` controls，不写长期 memory、不更新 status、不追加 `memory_applied`；`agentdeck memory apply-preview --suggestion-id <id>` 只能只读返回目标 memory 文件、是否会创建、proposed_append、显式 apply command 和 apply control，不得写 `.agentdeck/memory/*.md`、不得更新 suggestion status、不得追加事件；`agentdeck memory apply --suggestion-id <id> --confirm` 是当前唯一长期 memory 写入路径，必须要求 `--confirm`，追加同一份 proposed Markdown 到 `.agentdeck/memory/project.md` 或 `.agentdeck/memory/global.md`，更新 suggestion 为 `applied`，写入 `applied_at` / `applied_path`，并追加 `memory_applied` 审计事件；未知 suggestion、缺少 `--confirm` 或非 pending suggestion 必须拒绝且不得重复写入。`agentdeck leader chat --message "查看 memory 建议"` 必须进入 `mode=memory_suggestions`，嵌入只读 `memory_suggestions_card`，复用 `agentdeck memory suggestions` 的 pending queue 语义；`agentdeck leader chat --message "预览 memory 建议 mem_xxx"` 必须进入 `mode=memory_apply_preview`，嵌入只读 `memory_apply_preview_card`，复用 `agentdeck memory apply-preview` 的目标文件、proposed_append 和显式 apply command 语义；`agentdeck workbench` 必须从 pending `memory_suggestions[]` 派生同源 `memory_suggestions_card`，暴露 `agentdeck memory suggestions` inspect control、`apply_preview_command_template` 和 item 级 `apply_preview` / `apply_memory` controls。memory suggestion preview/apply-preview/list/chat/workbench 不得创建或修改 `.agentdeck/memory/*.md`，不得调用 provider、读取 tmux、发送输入、修改 approval/runtime state，不得自动注入 Leader/Worker prompt；只有显式 `memory apply --confirm` 可以落地长期 memory。

Applied memory context：已应用的 `.agentdeck/memory/project.md` / `global.md` 只能以 compact 摘要进入 ProjectView `memory`、workbench `memory_context_card` 和自然语言 `mode=memory_context`。摘要字段限制为 scope/path/exists/line_count/byte_count/content_hash/preview，不得包含全文，不得自动注入 API-backed 或 CLI-backed Leader prompt，不得注入 Worker dispatch prompt；`agentdeck leader chat --message "查看长期记忆"` 只能只读展示 `memory_context_card`、记录 chat turn 和审计事件，不得调用 provider、读取 tmux、创建 plan/action/approval/message/job/inbox 或改变 runtime/approval state。

Learning review MVP：`agentdeck learn review --plan-id <id>` 是 Hermes 式后台 reviewer 的只读前置面，只能复用现有 plan status、Leader summary、reply 和 artifact 事实，生成 GUI-ready `agentdeck skills suggest ... --source learn-review` 与 `agentdeck memory suggest ... --source learn-review` 后续命令；它不得写 `skill_suggestions[]`、不得写 `memory_suggestions[]`、不得创建/导入/load skill、不得写 `.agentdeck/memory/*.md`、不得调用 provider、不得读取 tmux、不得创建 plan/action/approval/message/job/inbox。真正进入 pending queue 仍必须由人类显式运行 suggestion 命令。`agentdeck learn review` 输出和自然语言 `learning_review_card` 都必须通过 `validate_learning_review_contract()` 守门；失败时不得打印半坏 JSON。`agentdeck leader chat --message "学习复盘 pln_xxx"` 必须进入只读 `mode=learning_review`，嵌入同源 `learning_review_card`，附带过滤到 `scope=learning_review` / `card=learning_review_card` 的 `control_registry_card`，selection 指向 `suggest_skill` control；它只记录 chat turn，不得执行 suggestion 命令、写 skill/memory queue、调用 provider、读取 tmux 或创建调度对象。`agentdeck contract learning-review` / `--example` 必须把这一路径作为 GUI 可发现契约暴露，并同步 `docs/contracts/learning-review-schema.md`、contract index、workbench `contracts_card`、README、HISTORY 和测试。

Loaded skill context 必须进入 ProjectView `skills` 摘要、workbench `skill_context_card`、自然语言 `mode=skill_context` 和真实 Leader provider planning prompt；传给 API-backed / CLI-backed Leader prompt 的只能是 compact 摘要（load_id、agent_id、purpose、name、source、path、content_hash、description、required_tools、risk），不得包含完整 `content_snapshot`。同一份 compact context 必须随 `leader plan`、自然语言 plan 和 `run --task` 固化到 plan record、ProjectView `plans.items[]` 和 `agentdeck plan status`，作为可审计 provenance。`agentdeck leader chat --message "查看已加载技能"` 只能展示已加载 skill、记录 chat turn 和审计事件，不得调用 provider、读取 tmux、安装或改写 skill、创建 plan/action/approval/message/job/inbox 或改变 runtime/approval state；`agentdeck leader chat --message "查看 skill 建议"` 必须进入 `mode=skill_suggestions`，嵌入只读 `skill_suggestions_card`，复用 `agentdeck skills suggestions` 的 pending queue 语义，只建议 inspect 命令，不得创建 `SKILL.md`、import、load、调用 provider、读取 tmux、创建 plan/action/approval/message/job/inbox 或改变 runtime/approval state；`agentdeck leader chat --message "创建 skill 建议 sgs_xxx"` 必须进入 `mode=skill_create_preview`，嵌入只读 `skill_create_preview_card`，复用 `agentdeck skills draft-preview` 的草稿/hash/目标路径语义，只返回显式 create 命令，不得写文件、更新 suggestion status、import/load skill、调用 provider、读取 tmux 或创建调度对象；`agentdeck leader chat --message "预览导入 skill <SKILL.md>"` 必须进入 `mode=skill_import_preview`，嵌入 `skill_import_preview_card`，复用 `agentdeck skills import-preview --path <SKILL.md>` 的 source/target/hash/overwrite/controls 语义，只建议显式 import 或 force import 命令，不得复制文件、load skill、调用 provider、读取 tmux、创建 plan/action/approval/message/job/inbox 或改变 runtime/approval state；`agentdeck leader chat --message "预览加载 skill <name> 给 <agent_id> 用于 <purpose>"` 必须进入 `mode=skill_load_preview`，嵌入 `skill_load_preview_card`，只建议显式 `agentdeck skills load ...` 命令，不得写 `skill_loads[]`、不得追加 `skill_loaded`、不得创建 plan/action/approval/message/job/inbox；provider planning prompt 也不得把 skill 当成 dispatch 或执行授权。

Worker dispatch skill 边界：`agentdeck dispatch` 和 `agentdeck approval dispatch` 只能把目标 agent 已显式加载的 skill `content_snapshot` 注入该 worker 的任务 prompt；不得注入其它 agent 的 skill。message、ProjectView `messages.items[]`、workbench `ledger_card.messages.items[]` 和 trace 必须保存 compact `prompt_skill_context` 作为 provenance，不能把该字段当成权限授权；审批、runtime safety 和 tool 权限仍由原有 gate 控制。

核心设计文档：

- `docs/architecture/multi-agent-terminal-design.md`
- `docs/roadmap/ultimate-goal-roadmap.md`
- `docs/reference-analysis/*.md`

## 当前技术栈

- Python 3.12
- Miniforge/conda 环境：`agentdeck`
- 标准库 CLI
- tmux runtime backend
- TOML 配置
- JSON/JSONL 状态骨架

## 环境准备

```bash
conda env create -f environment.yml
conda activate agentdeck
python -m pip install -e .
```

如果环境已存在：

```bash
conda activate agentdeck
python -m pip install -e .
```

## 常用命令

```bash
conda activate agentdeck
agentdeck doctor
agentdeck contract acp-runtime --example
agentdeck protocol acp preflight --agent <agent_id>
agentdeck project init
agentdeck status
agentdeck workbench
agentdeck controls
agentdeck agent list
agentdeck agent stop --agent planner
agentdeck agent release --agent coder --confirm
agentdeck agent assign-role --agent planner --role "architecture planning" --role-prompt "你负责架构规划和任务拆解。"
agentdeck leader chat --message "帮我设计自动 reply extraction"
agentdeck leader chat --message "查看 planner 输出"
agentdeck leader chat --message "查看 planner inbox"
agentdeck leader chat --message "追踪 planner 当前 inbox"
agentdeck leader chat --message "确认 planner 当前 inbox"
agentdeck leader chat --message "查看审批"
agentdeck leader chat --message "批准当前审批"
agentdeck leader chat --message "派发当前审批"
agentdeck leader chat-history
agentdeck leader plan --task "设计自动 reply extraction"
agentdeck leader review --plan-id pln_xxx
agentdeck leader next
agentdeck leader actions
agentdeck leader action --action-id act_xxx
agentdeck leader apply-action --action-id act_xxx
agentdeck plan list
agentdeck plan show --plan-id pln_xxx
agentdeck events --limit 20
agentdeck plan status --plan-id pln_xxx
agentdeck approval create-from-plan --plan-id pln_xxx
agentdeck approval list
agentdeck approval approve --approval-id apv_xxx
agentdeck approval approve-plan --plan-id pln_xxx --confirm
agentdeck approval dispatch --approval-id apv_xxx
agentdeck dispatch --agent planner --task "设计消息账本"
agentdeck worktree list
agentdeck worktree diff --message-id msg_xxx
agentdeck worktree merge --message-id msg_xxx --confirm
agentdeck worktree abandon --message-id msg_xxx --confirm
agentdeck worktree prune --confirm
agentdeck delegation grant --agent coder --prefix "node tests/" --confirm
agentdeck delegation list
agentdeck delegation revoke --delegation-id dlg_xxx --confirm
agentdeck agent boxes --agent coder
agentdeck agent release-box --agent coder --confirm
agentdeck boxes watch --confirm --iterations 3 --interval 5
agentdeck run-loop --plan-id pln_xxx --confirm --follow --max-waves 10 --interval 10 --release-boxes --merge-on-complete
agentdeck run-loop-host start --plan-id pln_xxx --confirm --max-waves 40 --interval 10 --release-boxes --merge-on-complete
agentdeck run-loop-host status
agentdeck run-loop-host stop --confirm
agentdeck plan rework --plan-id pln_xxx --confirm
agentdeck worktree merge-plan --plan-id pln_xxx --confirm
agentdeck storage shadow-enable --confirm
agentdeck storage shadow-status
agentdeck storage shadow-diff
agentdeck storage events-diff
agentdeck storage events-cutover --confirm
agentdeck storage events-rollback --confirm
agentdeck ui serve --port 8787
agentdeck inbox --agent planner
agentdeck reply --agent planner --message-id msg_xxx --text "status: completed"
agentdeck capture-reply --agent planner --message-id msg_xxx
agentdeck ack --agent planner --inbox-id inb_xxx
agentdeck trace --id msg_xxx
pytest tests/test_agent_cli.py -q
pytest tests/test_dispatch_cli.py -q
python -m compileall src
```

真实 ACP gate 只能由人类先在 AgentDeck 外安装并认证 adapter，再按 `docs/validation/phase2-acp-live-acceptance-sop.md` 显式运行。不得使用 `npx`、`npm`、`pip` 或自动下载/安装，不得自动修改认证；2026-07-12 PASS 证据在 `docs/validation/phase2-claude-agent-acp-vertical-slice.md`，未来重跑仍遵守同一边界。

所有开发、验证和 CLI 调试都应在 `agentdeck` 环境中执行。

安装为本地命令后：

```bash
conda activate agentdeck
agentdeck doctor
```

## 目录约定

```text
src/agentdeck/
  cli.py              # CLI dispatch
  config.py           # .agentdeck/config.toml
  models.py           # dataclasses for project/agent/message/job
  state.py            # JSON/JSONL state store
  runtime/            # runtime backend interfaces and tmux backend
  providers/          # LLM provider adapters
  orchestration/      # Leader/Worker planning skeleton
docs/
  architecture/
  reference-analysis/
```

Runtime state 默认写到 `.agentdeck/`，不要提交该目录。

## 开发规则

- 每次新增功能或用户可见行为变化都要 commit。
- 每次开发内容都要同步更新 `HISTORY.md`，并和对应代码/文档改动放在同一次 commit 中。
- 每次开发前先对照 `docs/roadmap/ultimate-goal-roadmap.md`，确认功能服务 Leader Agent、多 Agent 通信、可见 runtime、审批、恢复、可审计 skill 或 GUI 主线。
- GUI、自然语言入口和 Leader chat loop 应优先消费 `agentdeck status` 的 ProjectView 摘要；顶层 `leader.leader_backend` 必须作为当前配置 Leader provider/model 的 logical identity 来源，每个 `plans.items[].leader_backend` 必须作为历史 plan 的同源 provenance，不表示 tmux pane、readiness 或授权；不要直接散读 state 文件作为主入口。
- ProjectView 字段契约维护在 `docs/contracts/project-view-schema.md`；当前 `schema_version` 是 `project-view/v1`，任何 GUI、recovery 或自然语言入口改动都要保持该文档同步。
- ProjectView schema version 的源码单一来源是 `src/agentdeck/models.py` 的 `PROJECT_VIEW_SCHEMA_VERSION`；不要在 Python 源码里重复手写版本字符串。
- ProjectView contract discovery payload 和 example fixture 的源码入口是 `src/agentdeck/contracts.py`；CLI 只负责调用它。
- Contract index 维护在 `docs/contracts/contract-index-schema.md`，发现入口是 `agentdeck contract list`；`CONTRACT_INDEX_SPECS`、payload helper 和测试都在 `src/agentdeck/contracts.py` / `tests/` 中。新增 GUI-consumable contract 时必须同步索引。
- Doctor diagnostics contract 维护在 `docs/contracts/doctor-schema.md`，发现入口是 `agentdeck contract doctor`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。doctor configured_leader 必须携带同源 normalized `leader_backend`，该字段只表示 setup provenance，不表示 provider readiness、tmux pane 或执行授权；doctor contract discovery 必须公开 workbench、Leader chat 和 Leader review contract 入口，供 GUI setup 面板跳转到主控制面契约。
- Events timeline contract 维护在 `docs/contracts/events-schema.md`，发现入口是 `agentdeck contract events`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。
- Run contract 维护在 `docs/contracts/run-schema.md`，发现入口是 `agentdeck contract run`；payload、example fixture 和 `validate_run_start_contract()` 也在 `src/agentdeck/contracts.py`。
- Run-loop contract 维护在 `docs/contracts/run-loop-schema.md`，发现入口是 `agentdeck contract run-loop`；payload、example fixture、`RUN_LOOP_RESPONSE_FIELDS`、`RUN_LOOP_STOP_REASONS` 和 `validate_run_loop_contract()` 也在 `src/agentdeck/contracts.py`，并已注册进 `CONTRACT_INDEX_SPECS` / `agentdeck contract list`；纯 gate 诊断 `run_loop_gate()` 在 `src/agentdeck/autonomy.py`。修改 `agentdeck run-loop` 的 response 字段、`stop_reasons`、安全边界或 validator 时必须同步该 contract、contract index、README、HISTORY 和测试。
- Run-loop-all（并行调度器）contract 维护在 `docs/contracts/run-loop-all-schema.md`，发现入口是 `agentdeck contract run-loop-all`；`RUN_LOOP_ALL_RESPONSE_FIELDS`、`RUN_LOOP_ALL_PLAN_FIELDS`、`run_loop_all_example`、`run_loop_all_contract_response` 和 `validate_run_loop_all_contract()` 也在 `src/agentdeck/contracts.py`，并已注册进 `CONTRACT_INDEX_SPECS` / `agentdeck contract list`；引擎 `_run_loop_all` / `_busy_agents` 在 `src/agentdeck/cli.py`。修改 `agentdeck run-loop --all` 的 response 字段、round-robin/共享预算/skip-on-contention 语义、安全边界或 validator 时必须同步该 contract、contract index、README、HISTORY 和测试。
- Sequential workflow contract 维护在 `docs/contracts/workflow-schema.md`，发现入口是 `agentdeck contract workflow`；纯 helper 和 foreground runner 在 `src/agentdeck/workflow.py`，状态写入 `workflow_runs[]`，CLI 是 `agentdeck workflow preview|run|status|resume`。preview/status 必须只读且不实例化/读取 tmux；run/resume 必须要求 `--confirm`，冻结 plan hash/steps/agents/tasks/timeout，并只接受当前 turn 精确 `handoff_token` 的结构化回复。resume 不得重复派发已有 turn；任何 blocker/invalid/timeout/runtime loss/plan drift 必须停止后续步骤。workflow 不得 spawn、调用 provider、auto-ack、扩大权限或改变既有 run-loop/approval/capture 语义；修改字段、stop reason、安全边界或 validator 时必须同步 contract index、README、HISTORY、handoff 和测试。
- Leader chat response contract 维护在 `docs/contracts/leader-chat-schema.md`，发现入口是 `agentdeck contract leader-chat`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。自然语言 `leader_status` mode 必须嵌入 `leader_status_card` 并复用 `agentdeck leader status` 同源字段；自然语言 run-start mode 必须嵌入 `run_start_card` 并复用 `validate_run_start_contract()`；自然语言 run-progress mode 必须嵌入 `run_progress_card`、附带过滤到 `run_progress_card` 的 `control_registry_card`、selection 指向 `agentdeck plan status --plan-id <id>` inspect control，并复用同一个 validator；自然语言 learning_review mode 必须嵌入 `learning_review_card`、附带过滤到 `learning_review_card` 的 `control_registry_card`、selection 指向 `suggest_skill` control，并复用 `agentdeck learn review --plan-id <id>` 同源 response 字段；自然语言 summary mode 必须嵌入 `leader_summary_card`、附带过滤到 `leader_summary_card` 的 `control_registry_card`、selection 指向 `agentdeck leader summary --plan-id <id>` inspect control，并复用 `validate_leader_summary_contract()`；自然语言 ledger mode 必须嵌入 workbench 同源 `ledger_card` 和 `lineage_card`、附带过滤到 `ledger_card` 的 `control_registry_card`、selection 指向 `agentdeck workbench` inspect control，并复用 ledger card validator；自然语言 audit mode 必须嵌入 workbench 同源 `audit_card`、附带过滤到 `audit_card` 的 `control_registry_card`、selection 指向 `agentdeck events --limit 20` inspect control，并复用 audit card validator；自然语言 artifacts mode 必须嵌入同源 `artifacts_card`、附带过滤到 `artifacts_card` 的 `control_registry_card`、selection 指向 `agentdeck artifacts` inspect control，并复用 `validate_artifacts_contract()`；自然语言 memory suggestions mode 必须嵌入 `memory_suggestions_card` 并复用 `agentdeck memory suggestions` 同源字段；自然语言 memory apply preview mode 必须嵌入 `memory_apply_preview_card` 并复用 `agentdeck memory apply-preview` 同源字段；修改 leader_status/run/learning_review/summary/ledger/audit/artifacts/memory_suggestions/memory_apply_preview intent、嵌入 card 或 `intent_card` 控件时必须同步 leader-chat contract、README、HISTORY 和测试。
- 自然语言 trace mode 必须嵌入同源 `trace_card`、附带过滤到 `scope=trace` / `card=trace_card` 的 `control_registry_card`，并让 selection 指向 `agentdeck trace --id <id>` inspect control；它只读展示通信 lineage，不得执行 trace、capture、ack、dispatch、读取 pane 或发送 tmux 输入。
- Workbench snapshot contract 维护在 `docs/contracts/workbench-schema.md`，发现入口是 `agentdeck contract workbench`；payload、example fixture 和 `validate_workbench_contract()` 也在 `src/agentdeck/contracts.py`。Workbench 的 `agent_ready_card` 必须复用 `agentdeck agent ready` 响应形状和同一份 `runtime_card`，并通过 agent runtime ready validator 校验；它只读派生 startup readiness，不得 inspect tmux、spawn/refresh/dispatch、capture pane、send input 或写 state；`terminal_session_card` 必须从同一份 `runtime_card` 和项目 tmux 配置派生 project-level attach/select-pane affordances，并通过 terminal session validator 校验；每个 `terminals[]` item 必须暴露 `controls[]` 的 `kind=select_pane` / `safety=inspect` 控件，command 必须匹配同 item 的 `select_pane_command`，disabled item 必须保留 blocker；它只读展示终端入口，不得 attach tmux、select pane、capture、send、refresh、spawn/stop 或写 state；Workbench 的 `skill_suggestions_card` 必须从 pending `skill_suggestions[]` 派生，复用 `agentdeck skills suggestions` 语义，暴露 `agentdeck skills suggestions` / `agentdeck status` inspect controls，不得创建 `SKILL.md`、import、load、调用 provider、读取 tmux 或写 runtime/approval state；Workbench 的 `memory_suggestions_card` 必须从 pending `memory_suggestions[]` 派生，复用 `agentdeck memory suggestions` 语义，暴露 `agentdeck memory suggestions` / `agentdeck status` inspect controls、只读 `apply-preview` 控件和显式 `memory apply --confirm` 控件；渲染 workbench 不得创建或修改 `.agentdeck/memory/*.md`、注入 prompt、调用 provider、读取 tmux 或写 runtime/approval state；Workbench 的 `run_progress_card` 必须复用最新 plan 的 `agentdeck run --plan-id <id>` 响应形状和 `validate_run_start_contract()`，没有 plan 时为 `null`；`artifacts_card` 必须复用 `agentdeck artifacts` 响应形状和 `validate_artifacts_contract()`，暴露 `kind=inspect` / `command=agentdeck artifacts` / `safety=inspect` control，只展示 ProjectView artifact 摘要，不读取产物文件内容；`leader_summary_card` 只在 latest plan ready-to-summarize 时复用 `agentdeck leader summary --plan-id <id>` 和 `validate_leader_summary_contract()`，否则为 `null`。
- Controls contract 维护在 `docs/contracts/controls-schema.md`，发现入口是 `agentdeck contract controls`；payload、example fixture 和 `validate_control_registry_card_contract()` 也在 `src/agentdeck/contracts.py`，并复用 workbench control registry item 字段。controls contract 必须公开 card/filter/selection/item/group 字段；`filters.active_filter_keys` 必须按 `scope`、`card`、`query`、`control_id`、`enabled_only` 稳定顺序派生，供 GUI/TUI 渲染过滤 chip；无过滤时 `filters.item_count_before_filter` 必须等于 `item_count`；每个 control registry item 必须带 deterministic `control_id`，供 GUI/TUI 稳定渲染和审计关联，但它不是授权令牌，不能绕过 `enabled`、`safety` 或 `blocker`；`selection.blocker` 只解释未命中 control_id 等选择态，`selection.next_command` 只投影 enabled 选中项的命令，二者都不得用于执行授权。
- GUI 第一刀冻结在 `docs/superpowers/specs/2026-07-26-gui-readonly-web.md`：`agentdeck ui serve [--port N]` 用标准库 http.server 在固定 `127.0.0.1` 起只读单页（零新依赖），数据只经子进程白名单三条只读 CLI（`workbench`/`events --limit|--since`/`controls`）JSON 透传，`since` 作为单个 argv 元素传递（无 shell）；仅 GET（其它方法 405、未知路径 404），server 自身不写 state、不碰 tmux、不调 provider；执行面已按 safety 分级落地：`POST /api/inspect`（enabled+safety=inspect+无 --confirm 才执行）与 `POST /api/execute`（enabled+safety∈{explicit_user,explicit_runtime}+无占位符+`confirmed:true` 二步确认才执行，registry 命令合法携带的 --confirm 不拒绝），浏览器永远只发 control_id、命令文本只来自 live registry 重解析；**delegated 控件（run-loop/approval auto）保持展示复制不可从 web 执行**；其它 POST 405、未知路径 404。修改端点白名单、门禁或页面行为必须同步该 spec、HISTORY 和测试。
- SQLite 迁移路线冻结在 `docs/superpowers/specs/2026-07-26-sqlite-migration-route.md`；核心纪律：在 `state.py` 内部换后端，绝不另建平行内核；SQLite 连接必须在现有 `_protocol_mutation_lock` 内打开，永不新开第二把锁文件。当前处于 quarantine 影子阶段：`agentdeck storage shadow-enable --confirm` 显式建 `.agentdeck/state/state.db` 影子镜像（generic mirror v1 四表 + 指纹 pin + `storage_shadow_enabled` 事件），`StateStore.save()` 在锁内 JSON 落盘后全量镜像，**JSON 是唯一权威**，镜像失败只写 `logs/shadow-errors.jsonl` 绝不影响 JSON 写路径，`rm state.db` 即完全回滚；`storage shadow-status` 只读；`storage shadow-diff` 只读比对镜像重建（`reconstruct_state`，空集合靠 meta collections 清单恢复）与权威 JSON，逐集合点名 mismatch，漂移返回非 0、同步返回 0，零写——每轮 live 收尾必跑。events 双写（阶段 5a）钩在 `_append_event_bytes_locked` 成功路径末尾（同一把 mutation lock 内、journal 权威不变、失败只落 shadow-errors），`storage events-diff`（阶段 5b）只读后缀对齐比对 journal 与 events 表（启用前历史不算漂移），二者与 shadow-diff 一起构成每轮收尾三连检。events 切权威（阶段 5c,2026-07-28 user 拍板落地）：显式 `storage events-cutover --confirm` 在 mutation lock 内把 events 表按 journal 顺序整表重建、逐字节校验重建与 journal 相同后才把 meta `events_authority` 翻为 `sqlite`（任何不一致拒绝且零改动）；切换后 append 以表写为权威（失败冒泡）、同锁同步导出到 events.jsonl（导出失败只落 shadow-errors,events-diff 可见），`_event_journal_source()` 按 authority 从表重建 bytes,`events --since` 契约不变；`storage events-rollback --confirm` 验证导出零漂移后切回 journal；authority 读取异常一律 fail-safe 降级 journal。停同步导出（5d,2026-08-01 user 拍板落地,O(n²) 写消失点）：meta 新键 `events_export_mode` ∈ {`sync`(缺省), `on_demand`}，仅 authority=sqlite 时有意义，读取异常一律 fail-safe 按 `sync`（宁可多导出绝不静默停导出）；`on_demand` 下 `_append_event_bytes_locked` 的 sqlite 分支在权威表写完成后跳过 journal 同步导出（表写失败仍冒泡），`sync` 行为逐字节不变，journal 权威路径完全不受影响；显式 `storage events-export --confirm` 在 mutation lock 内把 events.jsonl 整文件重建为表字节流并逐字节校验（authority=journal 拒绝，审计 `storage_events_exported`）；显式 `storage events-export-mode --mode sync|on_demand --confirm` 要求 authority=sqlite，切到 `sync` 前先强制全量导出、切到 `on_demand` 只翻 meta，同值重复切换拒绝零写，审计 `storage_events_export_mode_updated`；`storage events-rollback --confirm` 在 `on_demand` 下先全量导出并校验再走既有零漂移验证与权威回切，并把 mode 重置为 `sync`；`storage events-diff` 在 `on_demand` 下改为前缀比对（文件是表字节流前缀即 ok，新增只读 `export_lag` 信息字段，重叠区内容不符仍是漂移），`sync` 行为不变；`shadow-status` 暴露 `events_export_mode`，且 `on_demand` 下显著标注 `rm state.db` **不再无损**（先 `events-export` 或 `events-rollback`）；停导出绝不自动发生，只有显式 `--confirm` 模式切换。镜像不应用 secrets 词法防火墙（`handoff_token` 等 state 键会误杀；防火墙留给后续规范化 canonical facts）。修改 shadow schema 必须重算指纹 pin 并同步 spec、HISTORY 和测试。
- Delegation contract 维护在 `docs/contracts/delegation-schema.md`，发现入口是 `agentdeck contract delegation`；fields/example/payload/`validate_delegation_list_contract()` 在 `src/agentdeck/contracts.py`，state writers `grant_delegation`/`revoke_delegation` 已注册。scoped 授权委托只自动化"人类本会按下的回车"：`delegation grant --agent <id> --prefix <p> --confirm` 写 `delegations[]` + `delegation_granted` 事件（未知 agent/空前缀/重复活跃对拒绝零写）；`delegation grant --agent <id> --mcp-server <s> --mcp-tool <t> --confirm` 是与 `--prefix` 互斥的 MCP 形态（双给/都不给/半对/空值/超出提取器字符集 `[A-Za-z0-9_-]+` 的值都拒绝零写——越界值 sentinel 永远匹配不到框，会在 walk-away 期间静默失效），写 `kind="mcp_tool"` 记录，缺省 `kind` 一律读作 `command_prefix`（既有记录零迁移）；`delegation list` 只读且输出前必须过 validator；`delegation revoke --confirm` 置 `revoked_at` + `delegation_revoked`；`agent boxes --agent <id>` 只读检测授权框并提取 `$ 命令`（codex 折叠长框、`$ ` 行不可见时回退从选项 2 自身的 "commands that start with \`…\`" 反引号内容跨行提取取末次出现，匹配时空白折叠兜住折行歧义；命令提取与 MCP 提取同标准区域锚定在挂起框上——旧框残留的 `$ ` 行绝不作为待批框命令被提取；绝不写 state、不发送输入）；MCP tool 授权框（第五类框，round 11 发现 #3）在命令提取无果时按框自身句式 "Allow the <server> MCP server to run tool <tool>?" 提取，硬保证是活框结构约束：折叠后句尾 `?` 必须紧跟活动选择器字形 `›1.`（待批框在句子正下方渲染 "› 1. Yes, proceed (y)"，折叠成单行历史的已答复句子后面是 "-> Yes" 等文本、绝不匹配；这同时保证只有预选项正是裸回车将按下的选项 1 时才提取成功），其上再叠两层启发式纵深：区域锚定（只搜尾窗倒数第二个 waiting marker 行之后——保留自身 footer 的旧框句子随之排除）与末次匹配（区域内取最后一个匹配），任何解析失败一律 None（fail-closed：解析失败绝不代按，绝不拿旧句子放行下方待批框），`agent boxes` 响应携带 `box_kind`/`mcp_server`/`mcp_tool`；`agent release-box --agent <id> --confirm` 仅当命中该 agent 活跃委托——命令框按前缀、MCP 框按 `(server, tool)` 双侧无归一化精确等值（grant 时字符集已强制）——才发回车并追加带 `box_kind`/`mcp_server`/`mcp_tool`/`waiting_hint`（屏上框证据）的 `auth_box_released` 审计事件；放行不变量不变：只在预选项上发裸回车，绝不选 option 2/3、绝不导航选项，重复弹框逐框放行逐次审计；`boxes watch --confirm --iterations N` 必须同时要求 `approval_mode=autonomous`（与 run-loop/approval auto 同门），有界扫描、逐次审计、未命中框只进 `skipped[]` 绝不代按；shell 包装命令（env 前缀赋值、for 循环、多命令链，round 12 发现 #3）由纯模块 `agentdeck.delegation_match` 做拆段+逐段覆盖匹配——每段必须命中该 agent 某条活跃委托前缀或属于内置固定胶水白名单（不可配置），且至少一段命中真实委托，命令替换/heredoc/输入重定向/后台 `&`/不配对引号/`eval`/`source`/非 `/tmp` 重定向一律硬拒，任一段不命中即整体拒（绝不部分放行），复合命令（含顶层 `;`/`&&`/`||`/`|`/换行或解析失败）绝不复用平前缀 startswith 的结论（`node tests/x.mjs; rm -rf /` 的首段会命中委托），解析不了即回落人工路径；三面响应与 `auth_box_released` 事件携带 `match_kind`（`prefix`/`composite`/`mcp_tool`/null）和仅复合匹配非空的 `matched_segments[]`（`{segment, via}`，`via` 为前缀原文或 `"glue"`）作为审计 provenance，不是权限授权。拒绝/esc 路径永不自动化；委托不是权限授权，建议只对本机只读验证前缀和任务 worktree 内 git 前缀 grant，绝不对 push/安装/网络变更前缀 grant（推荐清单见 delegation-schema.md 的 read-only starter pack 节——node tests/、node --check tests/、git status/log/diff、rg -n;仅文档推荐,仍逐条显式 grant,绝无批量/自动授予,`node -e`/`python -c` 类任意内容命令永远留人工）；MCP 委托只建议对只读性工具（hover/press_key/screenshot 类）grant，绝不对页面变更类工具（navigate/fill/evaluate_script 类）grant——AgentDeck 无法核验工具性质，该判断由人类在 grant 时负责。修改这些命令的字段、安全边界或 validator 时必须同步该 contract、contract index、README、HISTORY 和测试。
- Skills contract 维护在 `docs/contracts/skills-schema.md`，发现入口是 `agentdeck contract skills`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。它必须公开 `skills list/show/import-preview/import/load/suggest/suggestions/draft-preview/create` 响应字段、skill item/suggestion item 字段和 skill control 字段；修改 Skill Registry 输出、import-preview/import/load/suggest/suggestions/draft-preview/create safety 或 show/load/draft/create controls 时必须同步该 contract、contract index、README、HISTORY 和测试。
- Skill `planning_guidance` 是可选、compact、可审计的 Leader 规划元数据：通过 `_metadata_list` 解析，最多 8 条、每条最多 240 字符，缺省 `[]`；必须随 summary/load record/ProjectView/plan provenance 保存，但 provider prompt 只能包含 `agent_id=leader` load 的 guidance，绝不能包含完整 `content_snapshot`。built-in `sequential-handoff` 使用它指导固定线性 handoff plan，并建议 workflow preview → human-confirmed run；它必须显式 load，不得 auto-load、注入 Worker、授权执行或套用于 parallel/DAG/cycle/dynamic-step 需求。
- Skill dependencies（decision B slice 1，read-only）：`SKILL.md` frontmatter 可声明 `depends_on` 列表（复用 `_metadata_list`，与 `required_tools` 同源），只解析进 `SkillSnapshot.depends_on`，不改 `summary()`，仅作元数据，不自动 load、不自动 import。`agentdeck skills deps --name <name>` 只读解析 discovered skills（内置 + 项目本地）之间的传递依赖，输出 `mode=skills_deps`、`depends_on`、`resolved`、`missing`、`has_cycle`（+ `cycle` 路径）、拓扑 `order` 和 inspect-only `show` controls，并在打印前通过 `validate_skills_deps_contract()` 守门；它不 load、不 import、不写 state、不调用 provider、不读取 tmux，未知 `--name` 必须非 0 且无输出。`depends_on` 的版本约束（B-ver）、remote/marketplace 依赖是后续产品 fork，必须先 STOP + 询问 human。
- Skill dependency auto-load（decision B-auto）：`agentdeck skills load-plan --name <name> --agent <id>` 必须只读预览依赖 load 计划，复用 `resolve_skill_dependencies` + 该 agent 已有 `skill_loads`，输出 `mode=skill_load_plan`、deps-first 拓扑 `order`（每项 `{name,status,source}`）、`to_load`/`already_loaded`/`missing`/`has_cycle`/`cycle`/`blockers`/`can_load`/`confirm_command` 和 inspect-only `show` controls，经 `validate_skill_load_plan_contract()` 守门；它不写 state、不追加事件、不 load/import、不调用 provider、不读取 tmux，未知 skill/agent 必须非 0。`agentdeck skills load --name <name> --agent <id> --with-deps --confirm` 才能按 `to_load` deps-first 逐个 `store.record_skill_load` + `skill_loaded` 事件并追加一条 `skill_deps_loaded` 汇总，输出 `mode=skill_deps_loaded`；`--with-deps` 必须要求 `--confirm`（否则拒绝、零写），缺失依赖或依赖环必须拒绝且零写（绝不 auto-import——缺失依赖是硬 blocker，import 仍走既有显式 allowlist-gated 流程），绝不静默；单 skill `skills load`（无 `--with-deps`）行为必须逐字节保持不变。修改 load-plan/load --with-deps 输出、安全边界或 controls 时必须同步 skills contract、README、HISTORY、handoff 和测试。
- Skill dependency version pinning（decision B-ver）：`depends_on` 条目可选锁定内容 hash `name@sha256:<hex>`；纯 `name` 表示任意版本（行为不变）。resolver 必须用纯 helper `_parse_dep(entry)` 解释每个 raw 条目（首个 `@` 切分，空 pin 忽略），`SkillSnapshot.depends_on` 仍存 raw 条目。`resolve_skill_dependencies` 必须新增 `version_mismatch: list[{name, expected, actual}]`：当被锁定的依赖存在但 `content_hash` 与 pin 不符时记入，该依赖既不进 `resolved` 也不进 `missing`，且作为 blocker leaf 不递归；`resolved`/`missing`/`order`/cycle 语义在其它方面不变。`skills deps` 必须输出 `version_mismatch`（加入 `SKILLS_DEPS_RESPONSE_FIELDS` + validator），`skills load-plan` 必须把 `version_mismatch` 加入 payload 和 `blockers`（`"version mismatch: <name> expected <pin>"`，加入 `SKILL_LOAD_PLAN_RESPONSE_FIELDS` + validator），`can_load` 因此在 mismatch 时为 false，`skills load --with-deps --confirm` 必须像 missing/cycle 一样硬阻断、零写。pin 是纯内容 hash 等值——确定性、本地、无网络；不得 auto-fix、auto-import 或静默 load。修改 pin 解析、`version_mismatch` 输出、安全边界或 controls 时必须同步 skills contract、README、HISTORY、handoff 和测试。
- Skill dependency semver ranges（decision semver）：`SKILL.md` frontmatter 可声明 `version: X.Y.Z`（默认 `0.0.0`，加入 `SkillSnapshot.summary()` 和 `SKILLS_SKILL_ITEM_FIELDS` + example fixture）。`depends_on` 条目 `name@<spec>` 中，凡 `<spec>` 不以 `sha256:` 开头即为 **semver range**，与依赖声明的 `version` 比对（`sha256:` 仍走 B-ver 内容 hash，纯 `name` 仍表示任意版本）。支持子集：bare/`==` 精确、`>=` `>` `<=` `<`、caret `^X.Y.Z`（= `>=X.Y.Z` 且 `< (X+1).0.0`）、逗号 AND（`>=1.2,<2.0`，全部成立）；版本为 `MAJOR[.MINOR[.PATCH]]`，缺省补 0。不支持（`.x`/`*` 通配、`~`、pre-release、`||` OR）或任何无法解析的 range 一律 fail-safe 视为硬 blocker，绝不静默通过。comparator 是纯 stdlib `parse_version` + `version_satisfies`（`skills.py`），确定性、本地、无网络、不引第三方库。resolver 对存在依赖分类 spec：`sha256:` → 内容 hash，否则 → `version_satisfies(dep.version, spec)`，不满足即记入 `version_mismatch`（新增 `reason` 键：`content hash mismatch` / `version range not satisfied`；`name/expected/actual` 与 B-ver 兼容）并作为 blocker leaf 不递归；`version_mismatch` 继续经 `skills deps` / `load-plan` blockers / `load --with-deps` 硬阻断、零写。lockfile 是下一个独立 spec；remote/marketplace 依赖仍是 STOP + 询问 human 的 fork。修改 `version` frontmatter、comparator、resolver 分类、安全边界或 controls 时必须同步 skills contract、README、HISTORY、handoff 和测试。
- Skill dependency lockfile（decision lockfile）：`agentdeck skills lock --name <name>` 是显式写路径，复用 `resolve_skill_dependencies` + `discover_skills` 冻结该 skill 已解析的依赖树到 `.agentdeck/skill-locks/<name>.json`（专用目录，不在 `.agentdeck/skills/` 下，`discover_skills` 不会拾取），内容为 `{name, locked_at, dependencies:[{name, content_hash, version}]}`（`dependencies` = resolver deps-first `order` 去掉 root）；成功时写 lockfile 并追加一条 `skill_locked` 审计事件 `{name, dependency_count}`，输出 `mode=skill_locked`（字段 `SKILL_LOCK_RESPONSE_FIELDS`），经 `validate_skill_lock_contract()` 守门；依赖树存在任何 blocker（missing / cycle / version_mismatch）必须**拒绝**——不写 lockfile、不写事件、非 0，未知 `--name` 同样非 0 无写。re-lock 覆盖既有 lockfile（事件记录每次写）。`agentdeck skills lock-verify --name <name>` 是**全只读** drift 报告，输出 `mode=skill_lock_verify`（字段 `SKILL_LOCK_VERIFY_RESPONSE_FIELDS`：`locked/in_sync/changed/added/removed/blockers`），经 `validate_skill_lock_verify_contract()` 守门：无 lockfile → `locked=false` + hint + 退出 0；否则重新解析并与 lock diff（`changed` = hash/version 变化、`added` = 新增依赖、`removed` = 消失依赖，另带当前 `blockers`），`in_sync` 仅当四者皆空为真；它不写 state、不写事件、不改 lockfile。lockfile 本切片是**advisory** drift 检测，**不**改变 `skills deps`/`load` 的解析行为（enforce 是后续切片）；本地、无网络、无第三方库。下一个依赖项 remote/marketplace（C）必须 STOP + 询问 human，绝不在 loop 里开工。修改 lock/lock-verify 输出、lockfile 格式、安全边界或 controls 时必须同步 skills contract、README、HISTORY、handoff 和测试。
- Memory contract 维护在 `docs/contracts/memory-schema.md`，发现入口是 `agentdeck contract memory`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。它必须公开 `memory suggest/suggestions/apply-preview/apply` 响应字段、suggestion item 字段和 memory control 字段；修改长期记忆建议、只读预览、显式 apply 或 GUI controls 时必须同步该 contract、contract index、workbench contracts_card、README、HISTORY 和测试。Memory contract 是北极星学习层的一部分：允许内源/外源建议进入 pending queue，但不得绕过 `--confirm` 直接写 `.agentdeck/memory/*.md` 或注入 prompt。
- Learning review contract 维护在 `docs/contracts/learning-review-schema.md`，发现入口是 `agentdeck contract learning-review`；payload、example fixture 和 `validate_learning_review_contract()` 也在 `src/agentdeck/contracts.py`。它必须公开 `learn review` response 字段、skill/memory suggestion 字段和 control 字段；修改 `agentdeck learn review` 的建议命令、安全边界、validator 或 GUI controls 时必须同步该 contract、contract index、workbench contracts_card、README、HISTORY 和测试。Learning review contract 是北极星学习层的只读 reviewer 面：可以提出后续 `skills suggest` / `memory suggest` 命令，但不得自动入队、写 memory、创建或 load skill。
- Agent runtime contract 维护在 `docs/contracts/agent-runtime-schema.md`，发现入口是 `agentdeck contract agent-runtime`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`，并公开 ready/spawn-ready/terminal/capture/refresh 响应字段。
- Leader review response contract 维护在 `docs/contracts/leader-review-schema.md`，发现入口是 `agentdeck contract leader-review`；payload、example fixture 和 validator 也在 `src/agentdeck/contracts.py`。修改 `leader review` 的 `next_command` 或 `controls[]` 时必须同步该 contract，并保持 live 输出通过 `validate_leader_review_contract()` 守门。
- Leader summary response contract 维护在 `docs/contracts/leader-summary-schema.md`，发现入口是 `agentdeck contract leader-summary`；它是 `leader review` 的 `next_action=summarize` 后续只读聚合面，payload、example fixture 和 validator 也在 `src/agentdeck/contracts.py`。live summary、workbench 嵌入 `leader_summary_card` 和自然语言 summary mode 必须携带同一份 normalized `leader_backend`，该字段只表示逻辑 Leader 推理后端来源，不表示 tmux pane 或执行授权；修改 `leader summary` 的 `leader_backend`、`steps[]`、`artifacts[]` 或 `controls[]` 时必须同步该 contract，并保持 live 输出通过 `validate_leader_summary_contract()` 守门。
- Leader status contract 维护在 `docs/contracts/leader-status-schema.md`，发现入口是 `agentdeck contract leader-status`；payload 和 example fixture 也在 `src/agentdeck/contracts.py`。`agentdeck leader status` 必须先通过 ProjectView contract 守门，再复用 workbench 同源 `provider_health`，只读公开 logical Leader、provider readiness/setup、latest_plan、queue counts、recovery、next_command 和 controls；它不得调用 provider、读取 tmux pane、写 state、创建 plan/action/approval/message/job/inbox 或执行任何推荐命令。
- `agentdeck agent refresh` 是显式 runtime reconciliation 命令；只检查 state 中记录为 `running` 的 pane 是否仍存在，丢失时标记为 `stale` 并写入 `agent_runtime_stale` 事件，不发送 tmux 输入、不推断任务完成。
- `agentdeck agent ready` 是只读 multi-agent runtime readiness card；它必须复用 workbench `runtime_card`，公开 total/running/not_running/all_running、所有 not-running agent 的 `spawn_commands`、`spawn_ready_command`、显式 `next_command`、`refresh_command`、`dispatch_ready_command` 和 GUI-ready `controls[]`；多个 agent 未 running 时 `next_command` 必须是 `agentdeck agent spawn-ready --confirm`，只有一个 agent 未 running 时才是对应单 agent spawn，全部 running 时是 `agentdeck approval dispatch-ready --confirm`；不得写 state、追加事件、inspect tmux、spawn/stop/capture/send、refresh binding 或 dispatch approvals。
- `agentdeck workbench` 的 `operator_card.controls[]` 必须和同一张 operator card 的顶层字段对齐：preview/apply/explicit/capture_reply command 不得漂移，enabled 必须反映对应命令、`can_apply` 和 `blocker`，blocker 必须同步为 preview `null`、apply 顶层 `operator_card.blocker`、explicit/capture_reply 顶层 blocker 或缺 explicit command 的固定说明；`action_kind=approval_dispatch_ready` 时，`dispatch_ready` control 必须使用 `agentdeck approval dispatch-ready --confirm`，enabled 必须反映顶层 blocker 是否为空，且 blocker 必须等于 `operator_card.blocker`。
- `agentdeck agent spawn-ready --confirm` 是显式 runtime 批量启动命令；不带 `--confirm` 必须失败且不得写 state 或创建 pane；带 confirm 时只启动尚未 `running` 的 configured agents，跳过已 running agent，写入每个新 pane 的 `agent_spawned` 事件和一次 `agent_spawn_ready_completed` 事件，输出 `mode=agent_spawn_ready`、spawned/skipped 计数和逐 agent results。
- `agentdeck agent terminal --agent <id>` 是只读 visible pane 定位卡；它必须返回 tmux attach/select-pane 命令、capture/send/stop/inbox/refresh 命令和 terminal-card 专属 controls，其中 `kind=terminal` 必须指向 tmux attach command、`kind=select_pane` 必须指向 tmux select-pane command，不得 attach tmux、不读取 pane 输出、不发送输入、不写 state、不追加事件。
- `agentdeck workbench` 的 `runtime_card.agents[]` 必须公开 `terminal_command=agentdeck agent terminal --agent <id>`，并在 `controls[]` 中保留 `kind=terminal` / `Open terminal`；running agent 的 terminal control 可 enabled，未 running agent 的 terminal control 必须 disabled 且 blocker 为 `agent is not running`。
- `agentdeck controls` / workbench `control_registry[]` 必须保留 `scope=agent_ready` 的 `kind=inspect` / `kind=spawn_ready` / `kind=refresh_runtime` / `kind=dispatch_ready` 启动准备入口，以及 `scope=terminal_session` 的 `kind=attach_session` / `kind=open_controls` / `kind=refresh_runtime` 项目级终端入口，以及来自 `terminal_session_card.terminals[].controls[]` 的 `kind=select_pane` 单 pane 入口；也必须保留 `scope=terminal` / `card=terminal_card` 的 `kind=terminal` tmux attach、`kind=select_pane` tmux select-pane、capture/send/stop/inbox items，`scope=run_progress` / `card=run_progress_card` 的 plan_status/review/approval_queue/next/continue/workbench items、`scope=ledger` / `card=ledger_card` / `kind=inspect` 的 `agentdeck workbench` item、`scope=artifacts` / `card=artifacts_card` / `kind=inspect` 的 `agentdeck artifacts` item、`scope=audit` / `card=audit_card` / `kind=inspect` 的 `agentdeck events --limit 20` item、`scope=autonomous` / `card=control_mode_card` 的 `kind=approval_auto`（`agentdeck approval auto --confirm`、`safety=delegated`、只有 `approval_mode=autonomous` 时 enabled，否则 disabled 且 blocker 为 `autonomous mode is not enabled`）和 disabled `kind=run_loop`（`agentdeck run-loop --plan-id <id> --confirm`、`safety=delegated`、blocker 为 `requires --plan-id`）items，二者从 `control_mode_card.autonomous_actions[]` 派生，渲染这些 controls 只是 inspect-only surfacing、不是执行授权，两条命令仍必须由人类显式带 `--confirm` 执行；`scope=trace` / `card=trace_card` / `kind=inspect` 的 `agentdeck trace --id <id>` item、`scope=capture` / `card=capture_card` / `kind=inspect` 的 `agentdeck agent capture --agent <id> --lines 200` item、`scope=leader_summary` / `card=leader_summary_card` 的 summary/plan_status/review/trace items，以及 runtime scope 的 `kind=terminal` item，供 GUI/TUI 直接渲染运行进度、通信链路、产物入口、总结入口和终端工具条；attach/open/select_pane 是 inspect-only，refresh_runtime 是 explicit_runtime，不代表自动 attach tmux、select pane、capture pane、send input 或写 state 的许可。
- `validate_workbench_contract()` 必须逐项校验每个 `runtime_card.agents[]` item 及其嵌套 `controls[]` item，不能只验证第一条 runtime agent；后续 Agent 缺少 terminal/send/capture/control safety 字段时必须带数组索引报错。
- `agentdeck leader chat --message "启动 <agent>"`、`"刷新 runtime"`、`"发送给 <agent>：<text>"` 和 `"停止 <agent>"` 必须返回 `runtime_action_card`，以 `intent_card.embedded_card=runtime_action_card` 暴露项目级 refresh 或目标 agent、role、runtime_status、pane_id、preview_text、显式 spawn/refresh/send/stop command 和 inspect/spawn/refresh/send/stop controls；同一响应必须嵌入 `runtime_card`、`terminal_session_card` 和过滤到 `runtime_action_card` 的 `control_registry_card`，selection 指向 spawn/refresh/send/stop control，且 `selection.next_command` 必须等于 `runtime_action_card.command`；单 Agent spawn 还必须把 `startup_preview_card` 作为 companion checklist。该卡片只是 GUI-ready 启动/刷新/发送/停止前确认，不得自动 spawn pane、refresh runtime、发送 tmux 输入、停止 pane、创建 plan/action/approval/message/job/inbox 或修改 runtime state。
- `status.recovery` 必须把 `stale` runtime bindings 作为 `runtime_stale` 恢复状态暴露，`recommended_action.source=runtime` 且 `next_command=agentdeck agent refresh`；`pending.runtime_stale` 是 ProjectView recovery pending 契约字段。
- `status.recovery` 必须把 latest plan review 的 `wait_for_reply` 作为 `reply_waiting` 恢复状态暴露，前提是没有 pending leader action、approval、stale runtime 或 pending inbox item；`recommended_action.source=reply`、`target_id=<message_id>`，`next_command` 必须是显式 `agentdeck capture-reply --agent <id> --message-id <msg_id>`；`pending.reply_waiting` 是 ProjectView recovery pending 契约字段。
- `agentdeck leader chat --message "继续"` 在 recovery source 为 `runtime` 时必须嵌入同源 `runtime_card` 和 `terminal_session_card`，复用 workbench runtime/terminal session 字段规则，并让 `intent_card.embedded_card=continue_card`、`intent_card.secondary_embedded_cards=["runtime_card","terminal_session_card"]`；在 recovery source 为 `reply` / `reply_waiting` 时必须嵌入同源 `trace_card`，复用 `agentdeck trace --id <message_id>` 的通信 lineage 字段，并让 `intent_card.embedded_card=trace_card`，只建议显式 `agentdeck capture-reply --agent <id> --message-id <msg_id>`；它只展示恢复入口，不自动 refresh、spawn、stop、capture、capture-reply、attach/select-pane 或发送 tmux 输入。
- `agentdeck leader chat --message "查看 Leader 状态"` / `"刷新 Leader 状态"` / `"Leader 概览"` / `"leader status"` / `"leader refresh"` / `"leader overview"` 必须进入只读 `mode=leader_status`，嵌入与 `agentdeck leader status` 同源的 `leader_status_card`，并让顶层 `provider_health` 和 `next_command` 与该卡片对齐；`intent_card.embedded_card` 必须是 `leader_status_card`，refresh control 必须匹配 `leader_status_card.refresh_command`，inspect control 必须指向 `agentdeck leader status`，并且必须附带过滤到 `leader_card` 的 `control_registry_card`，selection 指向 `kind=refresh` 的同源 refresh control。该入口只可记录 chat turn 和审计事件，不得调用 provider、读取 tmux pane、创建 plan/action/approval/message/job/inbox、修改 runtime state 或执行推荐命令。
- `agentdeck leader chat --message "查看 runtime"` / `"查看终端"` 必须进入只读 `mode=runtime`，嵌入同一张 `runtime_card` 和同源顶层 `terminal_session_card`，并让 `intent_card.embedded_card=runtime_card`、`intent_card.secondary_embedded_cards=["terminal_session_card"]`，供 GUI/TUI 直接渲染项目级 tmux terminal strip，同时建议 `agentdeck agent list`；`secondary_embedded_cards` 只是渲染提示，不得成为第二套状态源或授权来源；`terminal_session_card` 只暴露 attach/select-pane/refresh affordances，其中每个 `terminals[].controls[]` select-pane 控件都必须是 inspect-only 且与 item command/blocker 对齐，不得代表自动 attach、select pane、capture、send、refresh 或写 state 的许可；`"打开 planner 终端"` / `"进入 coder pane"` 这类打开单个 visible pane 的意图必须进入只读 `mode=terminal`，嵌入 `terminal_card`，顶层 `next_command` 对齐 `terminal_card.attach_command`，附带过滤到 `scope=terminal` / `card=terminal_card` 的 `control_registry_card`，selection 指向 `kind=terminal` tmux attach control，且 `intent_card.secondary_embedded_cards` 必须包含 `control_registry_card`，不得 attach tmux、select pane、capture、send、stop 或写 runtime state；`"刷新 runtime"` 这类刷新绑定意图必须只建议显式 `agentdeck agent refresh`；`"启动所有 agent"` / `"启动全部 agent"` / `"prepare all agents"` 这类多 Agent 启动准备意图必须嵌入 `agent_ready_card` 和同源 `startup_preview_card`，复用 `agentdeck agent ready` 字段，并让 `intent_card.embedded_card=agent_ready_card`、`intent_card.secondary_embedded_cards=["startup_preview_card","runtime_card","terminal_session_card","control_registry_card"]`，供 GUI/TUI 同屏渲染启动清单、执行前 startup checklist、runtime 状态、项目级终端条和已选中的启动 control；`startup_preview_card` 必须展开待启动 agent、单 agent spawn 命令、terminal 命令、blocker 和 per-agent controls，并通过 leader-chat validator 校验 count/ready_count/blocked_count 与 controls 对齐；顶层 `next_command` 必须对齐到 card 的显式下一步：多个 agent 未 running 时是 `agentdeck agent spawn-ready --confirm`，单个未 running 时是对应单 agent spawn，全 running 时是 `agentdeck approval dispatch-ready --confirm`；`"启动 planner"` / `"spawn coder"` 这类明确启动单个 agent 的意图也必须进入 `mode=runtime`，以 `runtime_action_card(action=spawn)` 作为 primary card，返回过滤到 `runtime_action_card` 的 `control_registry_card` 且 selection 指向顶层 spawn control，在 `intent_card.secondary_embedded_cards` 中列出 `runtime_card`、`startup_preview_card`、`terminal_session_card` 和 `control_registry_card`，同时把 `startup_preview_card` 作为目标 agent checklist，但只能建议显式 `agentdeck agent spawn --agent <id>`；`"发送给 planner：继续"` / `"tell coder fix tests"` 这类给单个 running agent 发送输入的意图只能建议显式 `agentdeck agent send --agent <id> --text <text>`；`"停止 planner"` / `"stop coder"` 这类停止单个 running agent 的意图只能建议显式 `agentdeck agent stop --agent <id>`；目标未 spawn 时必须返回 `agent is not spawned: <id>` 且不能落入 plan/provider 路由；runtime command 建议必须在 `leader_explanation` / `intent_card` 中标记 `safety=explicit_runtime` 和 `requires_explicit_user=true`；它不创建 plan/action/approval/message/job/inbox，也不执行任何 runtime 操作。
- `agentdeck leader chat --message "查看 planner 输出"` / `"capture planner output"` 必须进入只读 `mode=capture`，嵌入带 `controls[]` 的 `capture_card`，附带过滤到 `scope=capture` / `card=capture_card` 的 `control_registry_card`，selection 指向同一条 `agentdeck agent capture --agent <id> --lines 200` inspect control；它只读取已 spawn 的 visible pane，不创建 plan/action/approval/message/job/inbox、不 ack、不 dispatch、不 capture reply、不发送 tmux 输入；未 spawn 的 agent 必须返回 `agent is not spawned: <id>`，不能落入 plan/review/provider 路由。`"捕获 planner 对 msg_xxx 的回复"` / `"capture reply from planner for msg_xxx"` 这类 capture-reply 意图也必须进入 `mode=capture`，但只能嵌入同源 `trace_card` 并建议显式 `agentdeck capture-reply --agent <id> --message-id <msg_id>`；`"捕获当前回复"` / `"回收当前结果"` 只能在 latest `leader_review.next_action=wait_for_reply` 时从 review 解析当前 agent/message，否则不得猜测目标；它不得读取 pane、不得写 reply、不得创建 message/job/inbox，`leader_explanation.action_kind` 必须是 `capture_reply`，`intent_card` next label 必须是 `Capture reply`，并标记 `safety=explicit_runtime` / `requires_explicit_user=true`。
- `agentdeck leader chat --message "派发当前审批"` 必须继续保持只读 `mode=approval`，但当存在 approved approval 时应嵌入 `dispatch_preview_card`，展示目标 agent/role/pane/task/dispatch_command/inbox_command/controls 和 blocker；同一响应必须嵌入 `approval_card` 和过滤到 `dispatch_preview_card` 的 `control_registry_card`，并让 selection 指向顶层 dispatch control；如果 blocker 存在，card 内 dispatch control、registry selection 和 `intent_card.controls[]` 的 next control 必须 disabled 并复用同一个 blocker；它只提供 explicit-runtime 执行前预览，不创建 message/job/inbox、不发送 tmux 输入。`agentdeck leader chat --message "派发所有已审批"` / `"dispatch all approvals"` 必须嵌入 `dispatch_batch_preview_card`，其中 `items[]` 复用单条 dispatch preview 字段并暴露每个 approval 的 inspect/dispatch controls 和 blocker，count/ready_count/blocked_count 必须和 items 一致；同一响应必须嵌入过滤到 `dispatch_batch_preview_card` 的 `control_registry_card`，selection 指向 `kind=dispatch_ready` control；顶层 `next_command` 应指向显式 `agentdeck approval dispatch-ready --confirm`，但 chat 本身不得执行派发。
- `agentdeck leader chat --message "查看队列"` / `"查看控制面"` 必须进入只读 `mode=queue`，嵌入 workbench 同源 `queue_card` / `operator_card` 并展示 next/apply/explicit controls；顶层 `next_command` 必须对齐 `operator_card` 的主命令，因此多条 approved approvals 时应推荐 `agentdeck approval dispatch-ready --confirm`，且 `operator_card.controls[]` 中对应 control 必须是 `kind=dispatch_ready`；同一响应必须嵌入过滤到 `operator_card` 的 `control_registry_card`，selection 指向顶层 `next_command` 对应的 operator control，`selection.next_command` 必须等于顶层 `next_command`，`intent_card.secondary_embedded_cards` 必须包含 `control_registry_card`；它不创建或应用 action、不审批、不派发、不 ack、不 refresh runtime、不发送 tmux 输入。
- `agentdeck leader chat --message "查看角色"` / `"查看分工"` 必须进入只读 `mode=role`，嵌入 workbench 同源 `role_card` 并展示 assign-role 命令和 `kind=assign_role` controls；同一响应必须附带过滤到 `scope=role` / `card=role_card` 的 `control_registry_card`，让 GUI/TUI 直接渲染角色编辑表单入口；`validate_workbench_contract()` 必须逐项校验每个 `role_card.agents[]` item，不能只验证第一条 role row；`"把 planner 设为 架构师"` / `"set reviewer role to QA"` 这类自然语言角色指派也必须进入 `mode=role`，但只能建议显式 `agentdeck agent assign-role --agent <id> --role <role> --role-prompt <prompt>`，并在 `leader_explanation` / `intent_card` 中标记 `action_kind=role_assign`、`safety=explicit_user`、`requires_explicit_user=true`；role_assign 响应的 registry selection 必须指向目标 agent 的 disabled `kind=assign_role` 模板 control，顶层 `next_command` 和 intent next control 才是填好参数后的显式命令；它不修改 `.agentdeck/config.toml`、不创建 plan/action/approval/message/job/inbox、不发送 tmux 输入。
- `agentdeck leader chat --message "让 planner 规划 README 更新"` / `"指派 coder 修复测试"` 这类自然语言任务指派必须进入 `mode=approval`，只创建一条 `source=leader_chat_task_assignment` 的 pending approval，并把 `next_command` 指向该 approval 的 `approve_command`；它可以写 approval queue 和 chat turn，但不得创建 plan/leader action，不得 approve/dispatch，不得创建 message/job/inbox 或发送 tmux 输入；`leader_explanation.action_kind` 必须是 `approval_create`，`intent_card.read_only=false`，下一步 control 仍必须是 `safety=explicit_runtime` / `requires_explicit_user=true`。
- `agentdeck leader chat --message "开始运行 <goal>"` / `"开始执行 <goal>"` / `"/run <goal>"` 必须进入 `mode=run_start`，复用 `agentdeck run --task <goal>` 的 provider planning、pending approvals 创建和 `run_start_card` 契约；它可以写 plan、approval queue、chat turn 和审计事件，但不得创建 `leader_actions[]`，不得 approve、dispatch、capture pane、ack inbox、创建 runtime message/job/inbox 或发送 tmux 输入；`leader_explanation.action_kind` 必须是 `run_start`，`intent_card.embedded_card=run_start_card`，下一步仍停在 `agentdeck approval list`。
- `agentdeck leader chat --message "查看运行进度"` / `"查看运行进度 <plan_id>"` 必须进入只读 `mode=run_progress`，省略 plan_id 时使用最新 plan，带 plan_id 时使用指定 plan，复用 `agentdeck run --plan-id <plan_id>` 的 `run_progress_card` 契约；没有任何 plan 时必须返回非 0 且不得创建 plan/action/chat turn；它只可记录成功查询的 chat turn，不得修改 plan/approval/runtime state，不得创建 `leader_actions[]`，不得 approve、dispatch、capture pane、ack inbox、创建 runtime message/job/inbox 或发送 tmux 输入；`intent_card.embedded_card=run_progress_card`，inspect control 必须指向实际使用的 `agentdeck run --plan-id <plan_id>`；同一响应必须附带过滤到 `scope=run_progress` / `card=run_progress_card` 的 `control_registry_card`，selection 指向该 plan 的 `kind=plan_status` inspect control，且 `intent_card.secondary_embedded_cards` 必须包含 `approval_card` 和 `control_registry_card`。
- `agentdeck leader chat --message "查看所有计划"` / `"计划看板"` / `"所有计划"` / `"查看计划列表"` / `"计划总览"` / `"plan board"` 必须进入只读 `mode=plan_board`，嵌入复用 `agentdeck plan board`（`_plan_board_payload`）响应形状并通过 `validate_plan_board_contract()` 校验的 `plan_board_card`（错误前缀 `plan_board_card: `），展示所有 plan 及其 gate 和 next command，不解析 plan id；顶层 `next_command` 必须是 `agentdeck plan board`，`intent_card.embedded_card=plan_board_card`，`leader_explanation.action_kind=plan_board`、`safety=inspect`、`requires_explicit_user=false`，intent next control 是 `kind=next` inspect control；没有 `scope=plan_board` 命令组，`control_registry_card=None`（对齐 plan/review mode）；它只记录一次 chat turn 和一条 `leader_chat_turn` 审计事件，不得调用 provider、读取/写入 tmux 或修改 plan/approval/runtime state。
- `agentdeck leader chat --message "浏览技能源"` / `"查看技能源"` / `"技能源"` / `"技能市场"` / `"技能目录"` / `"skill sources"` / `"skill catalog"` 必须进入只读 `mode=skills_catalog`，嵌入复用 workbench skills-catalog 卡片形状（`_skills_catalog_card(config)`，字段 `mode/source_count/total_skill_count/imported_count/sources[]`）的只读 `skills_catalog_card`，它是无参卡片，只概览配置的 `[skills] allowed_sources` 技能源；顶层 `next_command` 必须是 `agentdeck skills sources`，`intent_card.embedded_card=skills_catalog_card`，`leader_explanation.action_kind=skills_catalog`、`safety=inspect`、`requires_explicit_user=false`，intent next control 是指向 `agentdeck skills sources` 的 `kind=next` inspect control；没有 `scope=skills_catalog` 命令组，`control_registry_card=None`（对齐 plan_board mode）；它只记录一次 chat turn 和一条 `leader_chat_turn` 审计事件，不得调用 provider、读取/写入 tmux、import/load skill 或修改 plan/approval/skill/runtime state。
- `agentdeck leader chat --message "推进计划 pln_xxx"` / `"往前推 pln_xxx"` / `"驱动计划 pln_xxx"` / `"run-loop pln_xxx"` 必须进入只读 `mode=run_loop_preview`，嵌入 `run_loop_preview_card`（字段 `mode/plan_id/command/autonomous_enabled/safety/requires_explicit_user/blocker/enable_command/controls`），并把顶层 `next_command`、`leader_explanation.next_command`、`intent_card.next_command` 都对齐到显式 `agentdeck run-loop --plan-id <id> --confirm`；`intent_card.embedded_card=run_loop_preview_card`，`kind=next` control 必须 `safety=explicit_runtime`（不得 inspect），只有 autonomous 模式开启时才 enabled，否则 disabled 且 blocker 为 `autonomous mode is not enabled`，card 同时暴露 `enable_command`；同一响应必须附带过滤到 `scope=autonomous` / `card=control_mode_card` 的 `control_registry_card`，selection 指向 disabled `kind=run_loop` 模板 control（复用上一 slice 的 `scope=autonomous` 组，遵循 `role_assign` 模式：模板留在 palette，具体填好的命令只出现在顶层 `next_command` 和 intent next control），`intent_card.secondary_embedded_cards` 必须包含 `control_registry_card`；该 intent 必须要求 plan id（不得猜测），无 `pln_...` 的推进语句必须返回非 0 且不写任何内容，未知 plan id 必须报错；它只记录一次 chat turn 和一条 `leader_chat_turn` 审计事件，不得调用 provider、读取/写入 tmux、auto-approve、dispatch 或修改 approval/runtime/plan state；执行 run-loop 仍是人类显式命令。
- `agentdeck leader chat --message "查看账本"` / `"查看通信"` 必须进入只读 `mode=ledger`，嵌入 workbench 同源 `ledger_card` 和 `lineage_card`，展示 trace_commands、artifact 摘要与最近通信路径；`ledger_card.trace_commands[]` 必须覆盖 messages/jobs/replies/artifacts 摘要中的每条 `trace_command`，供 GUI 快速跳转；同一响应必须附带过滤到 `scope=ledger` / `card=ledger_card` 的 `control_registry_card`，selection 指向 `ledger_card.controls[]` 中 `command=agentdeck workbench` 的 inspect control，且 `intent_card.secondary_embedded_cards` 必须包含 `control_registry_card`；它不创建 plan/action/approval/message/job/inbox、不 ack、不 dispatch、不 capture reply、不读取 pane 输出、不执行 trace/workbench 命令、不发送 tmux 输入。
- `agentdeck leader chat --message "查看审计"` / `"最近事件"` 必须进入只读 `mode=audit`，嵌入 workbench 同源 `audit_card`，展示 latest_event、recent_events、event_count、`events_command=agentdeck events --limit 20` 和 inspect control，附带过滤到 `scope=audit` / `card=audit_card` 的 `control_registry_card`，selection 指向 `agentdeck events --limit 20` inspect control；`recent_events[]` 每项都必须保留 `event_id`、`event_type` 和 `created_at`，`event_count` 必须匹配列表长度；它只记录 chat turn 和对应审计事件，不创建 plan/action/approval/message/job/inbox、不 ack、不 approve、不 dispatch、不 capture、不读取 pane 输出、不发送 tmux 输入。
- `agentdeck leader chat --message "查看产物"` / `"输出文件"` / `"artifacts"` 必须进入只读 `mode=artifacts`，嵌入同源 `artifacts_card`，附带过滤到 `scope=artifacts` / `card=artifacts_card` 的 `control_registry_card`，selection 指向 `agentdeck artifacts` inspect control，并建议 `agentdeck artifacts`；它只记录 chat turn 和审计事件，不创建 plan/action/approval/message/job/reply/artifact/inbox、不调用 provider、不读取产物文件内容、不读取 pane、不发送 tmux 输入。
- `agentdeck leader chat --message "追踪 msg_xxx"` / `"trace job_xxx"` / `"追踪 art_xxx"` / `"查看 rep_xxx 链路"` 必须进入只读 `mode=trace`，嵌入同源 `trace_card` 并建议 `agentdeck trace --id <id>`；它不创建 plan/action/approval/message/job/inbox、不 ack、不 dispatch、不 capture reply、不读取 pane 输出、不发送 tmux 输入；未知 trace id 必须返回错误，不能落入 plan/review/provider 路由。
- `agentdeck leader chat --message "打开工作台"` / `"查看总览"` 必须进入只读 `mode=workbench`，嵌入完整 `workbench_card` 并通过 `validate_workbench_contract()` 校验；`agentdeck contract leader-chat` 必须公开 `terminal_session_card_fields`、`terminal_session_control_fields`、`terminal_session_item_fields` 和 `workbench_control_registry_item_fields`，供自然语言壳发现嵌入 `workbench_card.terminal_session_card` 的项目级终端条和 `workbench_card.control_registry[]` 的命令面板字段；它不创建 plan/action/approval/message/job/inbox、不 ack、不 approve、不 dispatch、不 refresh runtime、不 capture、不读取 pane 输出、不 attach tmux、不 select pane、不发送 tmux 输入。
- `agentdeck leader chat --message "帮助"` / `"help"` / `"你能做什么"` / `"命令面板"` 必须进入只读 `mode=help`，嵌入 `capability_card` 作为自然语言壳能力发现入口，并嵌入 `control_registry_card` 作为未来 GUI 命令面板快照；它必须暴露 plan/review/apply_action 这条 Leader 调度主线、policy 控制模式入口、skill_import_preview/skill_load_preview/skill_create_preview 等 skill lifecycle 入口以及只读控制面能力，每个 capability item 必须带 GUI-ready `controls[]`，其中 plan control 必须指向显式 `agentdeck leader plan --task <goal>`，review control 必须指向 `agentdeck leader review --plan-id <plan_id>`，policy control 必须指向显式 `agentdeck policy set-mode --mode <mode>`，skill_create_preview control 必须指向 `agentdeck leader chat --message "创建 skill 建议 <suggestion_id>"`；capability control 只能使用 `<goal>`、`<plan_id>`、`<action_id>`、`<agent_id>`、`<mode>`、`<provider>`、`<model>`、`<SKILL.md>`、`<name>`、`<purpose>`、`<suggestion_id>` 占位符，所有包含占位符的 control 必须 disabled，并提供与占位符类型匹配的 blocker；`control_registry_card` 必须从同一次 workbench snapshot 派生，保留 controls 的 safety/enabled/blocker，提供带 `active_filter_keys` 的 `filters` 元数据，提供从 `filters.control_id` 派生且带 blocker/next_command 的 `selection`，并提供由过滤后 items 派生的 `group_count` / `groups[]` 分组视图，且 `default_command` 必须指向独立入口 `agentdeck controls`；`命令面板 runtime enabled only` / `命令面板 搜索 terminal` / `命令面板 control_id <id>` 这类自然语言过滤、搜索和精确定位必须复用同一套 filters/selection contract，但 help mode 本身不调用 provider、不创建 plan/action/approval/message/job/inbox、不读取 pane、不发送 tmux 输入。
- `agentdeck leader chat --message "切换到审批模式"` / `"回到 ask 模式"` / `"开启 autonomous"` 必须进入 `mode=policy`，嵌入 workbench 同源 `control_mode_card` 并建议显式 `agentdeck policy set-mode --mode <mode>`；同一响应必须附带过滤到 `scope=policy` / `card=control_mode_card` 的 `control_registry_card`，selection 指向目标 mode 的 `kind=set_mode` control；enabled 的 ask/approve control 才能让 registry `selection.next_command` 等于顶层 `next_command`；autonomous 意图的顶层 `next_command` 必须是显式模板 `agentdeck policy set-mode --mode autonomous --confirm --allow-agent <id> --max-approvals <N>`，对应 `kind=set_mode` control 必须 disabled 并给出 `requires --allow-agent and --max-approvals` blocker，registry `selection.next_command=null`，intent next control 因含占位符也必须 disabled；它只记录 chat turn，不修改 `.agentdeck/config.toml`、不创建 plan/action/approval/message/job/inbox、不调用 provider、不发送 tmux 输入；真正启用有界自主模式仍必须由人类显式填好 allowlist 和预算后运行该命令。
- `agentdeck leader chat --message "切换 Leader 到 Codex CLI"` / `"使用 Claude Code 做 Leader"` / `"换成 DeepSeek Leader"` 必须进入只读 `mode=setup`，嵌入 workbench 同源 `provider_health`，并建议具体 `agentdeck leader set-provider --provider <provider> --model <model>`；`provider_health` 是正式 leader-chat response 字段，非 setup mode 可为 `null`，`agentdeck contract leader-chat` 必须公开 `provider_health_fields` 并复用 workbench provider health 字段列表；如果文本包含 `"要求可用"`、`"先预检"`、`"必须可用"` 或类似 require-ready 语义，建议命令必须追加 `--require-ready`；`leader_explanation.action_kind` 必须是 `provider_switch`，`safety=explicit_user`，`requires_explicit_user=true`，`intent_card` 必须以 `provider_health` 为 embedded_card、在 `secondary_embedded_cards` 中包含 `provider_switch_card`，并保留 `agentdeck doctor` inspect control；该响应还必须包含 `provider_switch_card`，公开 current provider/model、target provider/model、target normalized `leader_backend`、同源 target readiness、require_ready、command、diagnostics_command、mutates_config=false 和 explicit_user controls；`target_readiness.provider/model` 必须匹配 target provider/model，`target_readiness.leader_backend` 必须匹配 `target_leader_backend`，inspect control 必须匹配 `diagnostics_command`，provider control 必须按 `require_ready` 使用 `set_provider` 或 `guarded_set_provider`，且 `require_ready=true` 加 `target_readiness.ready=false` 时 guarded control 必须 disabled 并带 `target provider is not ready` blocker，同时必须提供与 `target_readiness.setup_commands` 对齐的 `kind=setup` controls；该模式只记录 chat turn，不修改 `.agentdeck/config.toml`、不调用当前或目标 provider、不创建 plan/action/approval/message/job/inbox、不发送 tmux 输入。
- `agentdeck leader chat --message "配置 Codex CLI Leader"` / `"登录 Codex CLI"` / `"安装 Claude CLI"` 必须进入只读 `mode=setup`，嵌入 workbench 同源 `provider_health`、结构化 `provider_setup_card`、provider-filtered `control_registry_card` 和目标 `provider_switch_card`，并建议 allowlist 中第一条 setup command，例如 `codex login` 或 `claude auth`；`provider_setup_card` 必须公开 target provider/model、setup_commands、recommended_command、recommended_control_id、followup_switch_command、require_ready、explicit_user controls 和 `mutates_config=false`，用于 GUI 渲染 setup checklist，但不得授权执行；live `provider_setup` 响应必须包含 `provider_setup_card`、`provider_switch_card` 和 `control_registry_card`，且 `intent_card.secondary_embedded_cards` 必须列出这三张卡；`provider_setup_card.recommended_command` 必须匹配顶层 `next_command`，`provider_setup_card.recommended_control_id` 必须指向 `command == recommended_command` 的 `setup_provider` control，并匹配 `control_registry_card.selection.requested_control_id`；`provider_setup_card.target_provider`、`target_model`、`require_ready` 和 `followup_switch_command` 必须与 `provider_switch_card` 对应字段一致，避免 GUI 渲染互相矛盾的 setup/switch 状态；`control_registry_card` 必须用该 setup command 对应的 `control_id` 选中推荐 control，使 `selection.next_command` 等于顶层 `next_command`；`provider_switch_card.command` 必须给出 setup 后的人类显式 `agentdeck leader set-provider --provider ...` 后续命令，但不得替代顶层 `next_command`；如果 setup 文本包含 `"要求可用"`、`"先预检"`、`"必须可用"` 或类似 require-ready 语义，follow-up `provider_switch_card` 必须使用 `require_ready=true`、command 追加 `--require-ready`，目标不可用时 guarded switch control 必须 disabled 并带 `target provider is not ready` blocker，同时保留来自 `target_readiness.setup_commands` 的 setup controls；`leader_explanation.action_kind` 必须是 `provider_setup`，`safety=explicit_user`，`requires_explicit_user=true`，`intent_card` 必须以 `provider_health` 为 embedded_card、在 `secondary_embedded_cards` 中包含 `provider_setup_card`、`provider_switch_card` 和 `control_registry_card`，next control label 必须是 `Run provider setup`；该模式只记录 chat turn，不修改 `.agentdeck/config.toml`、不调用当前或目标 provider、不创建 plan/action/approval/message/job/inbox、不发送 tmux 输入。
- 所有 `agentdeck leader chat` 响应都必须包含 `intent_card`，用于 GUI/自然语言壳解释 mode、matched_intent、route_source、embedded_card、read_only、next_command、requires_explicit_user 和 `controls[]`；`leader_explanation.next_command` 与 `intent_card.next_command` 都必须匹配顶层 `next_command`，`leader_explanation.requires_explicit_user` 必须匹配 `intent_card.requires_explicit_user`；intent controls 必须使用 `kind`、`label`、`command`、`safety`、`enabled`、`blocker`，其中 `kind=inspect` 必须是 `safety=inspect`，存在 `intent_card.next_command` 时必须有 `kind=next` control，且该 control 的 `command` 必须等于 `intent_card.next_command`；当 `intent_card.requires_explicit_user=true` 时，`kind=next` control 不得使用 `safety=inspect`，避免 GUI 把需要人类显式执行的主按钮渲染成只读检查；disabled control 必须提供 blocker，runtime、policy、approval、inbox ack 与只读观察类 action 的 next control 必须使用动作级 label，带 `<reason>` 等模板输入的 next control 必须 disabled 并提供匹配 blocker；新增 chat mode 时必须同步 `LEADER_CHAT_INTENT_CARD_FIELDS`、`LEADER_CHAT_INTENT_CONTROL_FIELDS`、`LEADER_CHAT_CAPABILITY_CARD_FIELDS`、`LEADER_CHAT_CAPABILITY_ITEM_FIELDS`、`capability_control_fields` discovery、validator、contract docs、README、HISTORY 和测试。
- `agentdeck contract leader-chat` 必须暴露 `capability_placeholder_fields` 和 `capability_placeholders`，让 GUI 机器发现 capability command 模板支持的 placeholder 白名单及 blocker；新增 placeholder 时必须同步 `LEADER_CHAT_CAPABILITY_PLACEHOLDERS`、validator、contract docs、README、HISTORY 和测试。
- Leader actions queue contract 维护在 `docs/contracts/leader-actions-schema.md`，发现入口是 `agentdeck contract leader-actions`；payload、example fixture 和 `validate_leader_actions_contract()` 也在 `src/agentdeck/contracts.py`。
- Leader action detail contract 维护在 `docs/contracts/leader-action-schema.md`，发现入口是 `agentdeck contract leader-action`；payload、example fixture 和 `validate_leader_action_contract()` 也在 `src/agentdeck/contracts.py`。
- Approval queue contract 维护在 `docs/contracts/approvals-schema.md`，发现入口是 `agentdeck contract approvals`；payload、approval queue example、dispatch-ready example、`validate_approval_contract()` 和 `validate_approval_dispatch_ready_contract()` 也在 `src/agentdeck/contracts.py`。
- Inbox queue contract 维护在 `docs/contracts/inbox-schema.md`，发现入口是 `agentdeck contract inbox`；payload、example fixture 和 `validate_inbox_contract()` 也在 `src/agentdeck/contracts.py`。
- Trace contract 维护在 `docs/contracts/trace-schema.md`，发现入口是 `agentdeck contract trace`；payload、example fixture 和 `validate_trace_contract()` 也在 `src/agentdeck/contracts.py`。Trace payload 必须包含只读 `controls[]`，其中 inspect control 指向 `agentdeck trace --id <id>`，供 GUI/TUI 直接渲染 lineage 检查入口。
- Artifacts contract 维护在 `docs/contracts/artifacts-schema.md`，发现入口是 `agentdeck contract artifacts`；payload、example fixture 和 `validate_artifacts_contract()` 也在 `src/agentdeck/contracts.py`。
- `src/agentdeck/contracts.py::validate_project_view_contract()` 是 ProjectView-like payload 的 v1 基础契约校验入口。
- `agentdeck status` 必须在输出 JSON 前调用 `validate_project_view_contract()` 自校验；校验失败时返回非 0 且不得输出半坏的 ProjectView。
- `agentdeck contract list` 是只读契约总目录，供 GUI/TUI 启动时读取所有 contract discovery 命令、example 命令和文档路径；不得读取或修改 live state。
- `agentdeck contract project-view` 是只读契约发现入口，供 GUI 或外部集成读取 schema version、契约文档路径和关键字段列表；`--example` 会返回稳定 ProjectView 示例，不代表 live state。
- `agentdeck contract events` 是只读契约发现入口，供 GUI 或外部集成读取事件时间线 response/cursor/event item 字段；`--example` 会返回稳定 events timeline 示例，不读取或修改 live state。
- `agentdeck contract leader-chat` 是只读契约发现入口，供 GUI 或外部集成读取 `leader chat` 响应字段和 `leader_explanation` 字段；`--example` 会返回稳定 chat 响应示例，不读取或修改 live state。
- `agentdeck contract leader-status` 是只读契约发现入口，供 GUI 或外部集成读取 `leader status` 状态卡字段；`--example` 会返回稳定状态卡示例，不读取或修改 live state。
- `agentdeck contract continue` 是只读契约发现入口，供 GUI 或外部集成读取 `agentdeck continue` 的恢复卡片字段；`--example` 会返回稳定 continue card 示例，不读取或修改 live state。
- `agentdeck contract workbench` 是只读契约发现入口，供 GUI 或外部集成读取 `agentdeck workbench` 的一屏快照字段；`--example` 会返回稳定 workbench 示例，不读取或修改 live state。
- `agentdeck contract controls` 是只读契约发现入口，供 GUI 或外部集成读取 `agentdeck controls` 的 command palette card 字段和 item 字段；`--example` 会返回稳定 control registry card 示例，不读取或修改 live state。
- `agentdeck contract leader-actions` 是只读契约发现入口，供 GUI 或外部集成读取 Leader action queue 字段；`--example` 会返回稳定队列示例，不读取或修改 live state。
- `agentdeck contract leader-action` 是只读契约发现入口，供 GUI 或外部集成读取单个 Leader action 详情字段；`--example` 会返回稳定 action detail 示例，不读取或修改 live state。
- `agentdeck contract approvals` 是只读契约发现入口，供 GUI 或外部集成读取人类审批队列字段和 dispatch-ready 响应字段；`--example` 会返回稳定 approval queue 与 dispatch-ready 示例，不读取或修改 live state。
- `agentdeck contract inbox` 是只读契约发现入口，供 GUI 或外部集成读取单 agent mailbox 字段；`--example` 会返回稳定 inbox 示例，不读取或修改 live state。
- `agentdeck contract trace` 是只读契约发现入口，供 GUI 或外部集成读取通信 lineage 的 message/attempt/job/reply/artifact/inbox 字段；`--example` 会返回稳定 trace 示例，不读取或修改 live state。
- `agentdeck contract artifacts` 是只读契约发现入口，供 GUI 或外部集成读取产物索引字段；`--example` 会返回稳定 artifacts 示例，不读取或修改 live state。
- `agentdeck trace --id <id>` 输出 JSON 前必须通过 `validate_trace_contract()` 自校验；失败时返回非 0 且不得输出半坏 trace。
- `agentdeck artifacts` 输出 JSON 前必须通过 `validate_artifacts_contract()` 自校验；它只返回同源 ProjectView artifact 摘要和 trace 契约入口，不读取产物文件内容、不读取 pane、不调用 provider、不写 state。
- `agentdeck leader chat` 输出 JSON 前必须通过 `validate_leader_chat_contract()` 自校验；校验失败时返回非 0、不得输出半坏 chat response，并必须写入 `leader_errors[]` 和 `leader_chat_contract_failed` 事件。
- `agentdeck leader actions` 输出 JSON 前必须通过 `validate_leader_actions_contract()` 自校验；校验失败时返回非 0 且不得输出半坏 action queue；每个 action item 必须公开 `controls[]`、`preview_command`、can_apply、apply_command、explicit_command、apply_blocker 和 is_recommended。
- `agentdeck leader action --action-id <id>` 输出 JSON 前必须通过 `validate_leader_action_contract()` 自校验；校验失败时返回非 0 且不得输出半坏 action detail；detail 必须公开 `preview_command`、can_apply、apply_command、explicit_command 和 apply_blocker。
- `agentdeck approval list` 输出 JSON 前必须通过 `validate_approval_contract()` 自校验；校验失败时返回非 0 且不得输出半坏 approval queue；每个 approval item 必须公开 `controls[]`、`preview_command`、approve/reject/dispatch commands、can_dispatch 和 dispatch_blocker。
- `agentdeck inbox --agent <id>` 输出 JSON 前必须通过 `validate_inbox_contract()` 自校验；校验失败时返回非 0 且不得输出半坏 inbox queue；每个 inbox item 必须公开 `controls[]`、`preview_command`、trace_command、ack_command、is_head、can_ack 和 ack_blocker。
- `agentdeck events --limit <n>` 是只读事件时间线入口，用于审计和 GUI 最近事件列表；`agentdeck events --since <event_id>` 返回 cursor 之后的审计事件和 cursor metadata，cursor 由 GUI/调用方持有，不得写入 AgentDeck state。
- `agentdeck status` 的 `recovery` 是默认恢复入口，必须保持只读，并暴露 status/reason/next_command/recommended_action/pending/leader_action/latest_event/recent_events；`recommended_action` 必须说明 label/command/safety/requires_explicit_user/source/target_id。
- 当没有 pending action、approval 或 inbox item 但存在 `leader_errors[]` 时，`agentdeck status.recovery` 必须返回 `status=leader_error` 和 inspect 型 recommended_action。
- `agentdeck status.recovery.pending` 必须包含 `leader_errors` 计数，供 GUI 统一展示 Leader 错误数量。
- `agentdeck contract project-view` 必须通过 `recovery_pending_fields` 暴露 `recovery.pending` 必备字段；validator 必须拒绝缺失字段的 ProjectView。
- `agentdeck status` 的 `chat_turns.items` 必须保留 action_id/action_kind，供 GUI 从自然语言 turn 跳转到 action。
- `agentdeck status` 的 `leader_actions` 必须保留 recommended_action_id，每个 `items[]` item 都必须保留 controls[]/preview_command/can_apply/apply_command/explicit_command/apply_blocker/is_recommended，供 GUI 和对话层展示安全动作预览、执行按钮与当前推荐项。
- `agentdeck status` 的 `messages.items[]`、`jobs.items[]`、`replies.items[]` 和 `artifacts.items[]` 每个 item 都必须保留 `trace_command`；`messages.items[]` 还必须保留 compact `prompt_skill_context`，让 GUI 不解析 worker prompt 也能看到 dispatch 时注入的 skill provenance；`messages.items[]` 同时必须保留 `worktree_path`/`worktree_branch`/`worktree_base_branch` 任务 worktree provenance（同 plan 更早 step 已有任务分支时，新 worktree 从该分支尖起建并记录 base——review step 由此在自己的隔离 worktree 检出实现产物）（worktree 模式 agent 且真实 git 仓库时由 dispatch 创建并记 `worktree_created` 事件，创建不成时二者为 null 并记 `worktree_skipped` 事件——可审计降级而非静默 fallback；该 provenance 不是权限或执行授权）；`agentdeck contract project-view` 必须公开对应 item field lists，validator 必须拒绝缺失 trace 入口或 message skill provenance 字段的 summary item。
- `agentdeck continue` 是顶层只读恢复入口；它必须先通过 ProjectView contract 守门，再通过 `validate_continue_contract()` 自校验，最后返回 recovery-driven 下一步卡片；当多条 approvals 已 approved 时，card 级 `next_command` 和 `recommended_action.command` 必须提升为显式 `agentdeck approval dispatch-ready --confirm`；当 recovery 是 `reply_waiting` 时必须展示显式 `capture-reply` 命令；但不得写 state、创建 action、apply action、dispatch、capture reply 或发送 tmux 输入；`agentdeck contract continue` 必须公开 `continue_card_fields`。
- `agentdeck workbench` 是 GUI/TUI 优先的一屏只读快照；它必须先通过 ProjectView contract 守门，再组合 project_view、leader_actions、leader_card、provider_health、runtime_card、agent_ready_card、terminal_session_card、role_card、ledger_card、lineage_card、queue_card、operator_card、audit_card、artifacts_card、leader_summary_card、contracts_card、control_mode_card、change_summary、recovery、continue_card、active_queue_source、run_progress_card、inbox_card、leader_inbox_card、approval_card 和 leader_action，并通过 `validate_workbench_contract()` 自校验；`control_mode_card` 必须从 Leader approval_mode 派生 ask/approve/autonomous 授权梯度，workbench 默认只读展示，不得自动修改策略或放权执行；`control_mode_card.active_controls[]` 必须包含 ask/approve/autonomous 三个具体 `set_mode` 控件，当前模式必须 disabled 并给出 `already current mode` blocker，ask/approve 控件必须使用 `safety=explicit_user`（切换 approval_mode 是配置写操作，available_modes 里的 safety 只描述模式自身性格；2026-07-26 live 发现 approve/autonomous 模式下 enabled 的 ask 控件曾用 inspect 违反注册表契约），autonomous available_mode 现在 enabled（有界自主模式已实现），但 autonomous `set_mode` 控件必须 disabled 并给出 `requires --allow-agent and --max-approvals` blocker、command 为显式模板 `agentdeck policy set-mode --mode autonomous --confirm --allow-agent <id> --max-approvals <N>`，`set_mode_command_template` 只能作为表单辅助；`agentdeck policy set-mode --mode ask|approve` 只写 `leader.approval_mode`，`agentdeck policy set-mode --mode autonomous --confirm --allow-agent <id> --max-approvals <N>` 是有界自主模式入口，必须校验 `--confirm`、非空 allowlist、`--max-approvals >= 1` 和已知 agent，通过后写 `leader.approval_mode=autonomous` 和 `[autonomous]` allowed_agents/max_approvals 并追加 `policy_mode_updated`，否则追加 `policy_mode_rejected` 并不改配置；`agentdeck approval auto --confirm` 是唯一自主执行入口，只在 autonomous 模式下自动批准 allowlist 内、预算内的 pending approvals 并派发给已有 running pane 的 agent（不 force-spawn），停在 dispatch，逐步审计（`approval_decided source=autonomous` + `approval_auto_completed`，可在 `agentdeck history` 查看）；`control_mode_card` 还必须携带 `autonomous_actions[]`（正好两条），把已实现的自主命令以只读 GUI control 形式暴露：`kind=approval_auto`（`agentdeck approval auto --confirm`、`safety=delegated`、只有 `approval_mode=autonomous` 时 enabled，否则 disabled 且 blocker 为 `autonomous mode is not enabled`）和 disabled `kind=run_loop`（`agentdeck run-loop --plan-id <id> --confirm`、`safety=delegated`、blocker 为 `requires --plan-id`），它们必须流入 `control_registry[]` 的 `scope=autonomous`，`validate_workbench_contract()` 逐项校验且要求 disabled item 带 blocker；渲染 enabled control 只是 surfacing、不是执行授权；`leader_card` 必须从 ProjectView leader 派生 agent_id/provider/model/approval_mode/api_backed、同源 normalized `leader_backend`、chat/continue/review/actions/status 入口命令和 controls[]，chat/review 模板 controls 必须 disabled 并提供 blocker，continue/actions/refresh/leader_status/status controls 必须是只读 inspect 入口，且不得暴露 API key 或调用 provider；`provider_health` 必须从 Leader provider 和本地环境变量派生 supported/ready/missing_env/detail/doctor_command/doctor_contract/setup_commands/controls[]，只能暴露 env 名称和显式 `agentdeck leader set-provider` 切换命令，不能暴露密钥值或调用 provider；provider controls 必须使用 `kind=set_provider`、`safety=explicit_user`，当前 provider disabled 并给出 `already current provider` blocker；`runtime_card` 必须从 ProjectView agents/runtime 派生并公开 agent role/provider/workspace/status/pane/session/cwd、spawn/stop/capture/send template/inbox 命令和 controls[]，其中 capture 只读，send/spawn/stop 类 runtime control 只能由人类显式触发；`agent_ready_card` 必须从同一张 runtime_card 派生 total_count/running_count/not_running_count/all_running、spawn_commands、spawn_ready_command、refresh_command、dispatch_ready_command 和 next_command，多个未 running agent 时 next_command 必须是 `agentdeck agent spawn-ready --confirm`，一个未 running agent 时必须是对应 single spawn，全部 running 时必须是 `agentdeck approval dispatch-ready --confirm`；`terminal_session_card` 必须从同一张 runtime_card 和 tmux config 派生 session_name、attach_command、running_count、agent_count、open_terminals_command、refresh_command、controls[] 和 terminals[]，其中 attach_session/open_controls controls 必须是 inspect，refresh_runtime control 必须是 explicit_runtime 并匹配 refresh_command，每个 enabled terminal 必须有 select_pane_command，未 running terminal 必须 disabled 并给出 blocker；`role_card` 必须从 ProjectView agents 派生 role/provider/workspace_mode/role_prompt/assign_command/controls[]，role controls 必须使用 `kind=assign_role`、`safety=explicit_user`，模板命令缺少 role/role_prompt 时必须 disabled 并给出 blocker；`ledger_card` 必须从 ProjectView messages/jobs/replies/artifacts/inbox 派生并保留 trace_commands；`artifacts_card` 必须复用 `agentdeck artifacts` response shape 与 `validate_artifacts_contract()`，只展示 ProjectView artifact 摘要和 trace 入口，不读取产物文件内容、不读取 pane、不调用 provider、不写 state；`leader_summary_card` 必须只在 latest plan ready-to-summarize 时复用 `agentdeck leader summary --plan-id <id>` response shape 与 `validate_leader_summary_contract()`，否则为 `null`；它只聚合已有 replies/artifacts/trace，不调用 provider、不读取 pane、不 capture reply、不写 state；`lineage_card` 必须从 ledger 摘要和可见 inbox cards 派生最近通信路径，保留 message/job/reply/inbox id、双方 actor/agent、task、status 和 trace_command，且 message_count/job_count/reply_count/inbox_count 必须覆盖 recent_paths 中携带对应 id 的路径数，且不得成为第二套 ledger 或执行入口；`queue_card` 必须从 leader_actions/approvals/inbox/recovery next_command 派生队列总览，且 leader_actions/approvals/inbox 子摘要必须匹配同一份 ProjectView snapshot；`operator_card` 必须从 recovery/recommended_action 和当前 active queue 派生，公开 controls[]/command/preview_command/safety/requires_explicit_user/apply_command/explicit_command/blocker 等人类操作字段，且 preview/apply/explicit/capture_reply controls 的 command 必须分别匹配顶层 preview_command/apply_command/explicit_command，enabled 也必须分别从 preview_command、can_apply+apply_command、explicit_command+blocker 派生；当 recovery 指向 approved approval dispatch 时，`operator_card` 必须从 ProjectView agents/runtime 派生 blocker，目标 agent 没有 running pane 时必须禁用 explicit control；当多条 approvals 已 approved 时，`operator_card.action_kind` 可以提升为 `approval_dispatch_ready`，`command`/`explicit_command` 必须指向 `agentdeck approval dispatch-ready --confirm`，匹配 control 必须使用 `kind=dispatch_ready`，并且仍不得自动执行；`control_registry` 必须从 leader/provider/policy/agent_ready/terminal_session/role/runtime/inbox/operator controls 派生为只读命令面板索引，保留 scope/card/kind/label/command/safety/enabled/blocker/agent_id，leader scope 必须保留 `kind=refresh`、`kind=leader_status` 和 `kind=status` items，provider scope 必须保留 `kind=set_provider` items，agent_ready scope 必须保留 `kind=inspect`、`kind=spawn_ready`、`kind=refresh_runtime` 和可选 `kind=dispatch_ready` items，terminal_session scope 必须保留 `kind=attach_session` / `kind=open_controls` / `kind=refresh_runtime` / `kind=select_pane` items，role scope 必须保留 `kind=assign_role` items，inbox scope 必须保留 `inbox_card.items[].controls[]` 和 `leader_inbox_card.items[].controls[]` 的 preview/ack items，且不得成为第二套状态源或绕过 control 的 safety/blocker；`audit_card` 必须从 recovery latest/recent event summary 派生并公开 events_command；`contracts_card` 必须公开 `agentdeck contract list`、contract index schema、run/workbench/controls/agent-runtime/leader-chat/leader-review/project-view/events/doctor contract 入口，且不得读取 state 或执行任何 contract 命令；`change_summary` 必须按可选 `--since-event <event_id>` 从审计事件账本临时计算，不得保存 cursor 到 state；不得写 state、创建 chat turn、ack、approve、dispatch、capture reply、读取 pane 输出或发送 tmux 输入；`agentdeck workbench --watch --since-event <event_id> --interval <seconds>` 必须输出同一契约形状的 JSONL 状态流，每行都通过 `validate_workbench_contract()`，并可用 `--iterations <n>` 有界退出；`agentdeck contract workbench` 必须公开 `snapshot_fields`、`leader_card_fields`、`leader_control_fields`、`control_mode_card_fields`、`control_mode_option_fields`、`control_mode_control_fields`、`provider_health_fields`、`runtime_card_fields`、`agent_ready_card_fields`、`terminal_session_card_fields`、`terminal_session_control_fields`、`terminal_session_item_fields`、`runtime_agent_fields`、`runtime_control_fields`、`role_card_fields`、`role_agent_fields`、`ledger_card_fields`、`lineage_card_fields`、`lineage_path_fields`、`queue_card_fields`、`operator_card_fields`、`run_progress_card_fields`、`audit_card_fields`、`artifacts_card_fields`、`artifact_summary_fields`、`artifact_item_fields`、`leader_summary_card_fields`、`contracts_card_fields`、`change_summary_fields`、`control_registry_item_fields`，且 `contracts_card_fields` 必须包含 `controls_contract`。
- `terminal_session_card.terminals[]` 的每个 item 必须带 `controls[]` 的 `kind=select_pane` / `safety=inspect` 控件，并且这些 item-level controls 必须进入 `control_registry[]` / `agentdeck controls` 的 `scope=terminal_session`；running item 的 control command 必须等于同 item 的 `select_pane_command` 且 enabled，未 running item 的 control 必须 disabled、command 为 `null` 并复用 `agent is not running` blocker；这些 controls 只供 GUI/TUI 渲染按钮，不授权自动 select-pane。
- `provider_health.provider_backend` / `provider_health.provider_transport` 必须是 GUI-ready provenance 字段：fake 使用 `local/local`，API-backed provider 使用 `api/http`，CLI-backed provider 使用 `cli/subprocess`，未知 legacy provider 使用 `unknown/unknown`；这些字段不得作为授权、dispatch 或 runtime 执行依据。
- `provider_health.leader_backend` 必须复用 ProjectView/workbench/run/review/summary/doctor 的 normalized logical Leader identity，用于 setup/provider switch GUI 区分 API-backed、CLI-backed 和 fake Leader reasoning backend；该字段不得作为 readiness、tmux pane 绑定、dispatch 或 runtime 执行依据。
- `provider_health.command_path` 必须是 GUI-ready 字段：API-backed provider 使用 `null`，CLI-backed provider 可用时暴露解析后的 `codex` / `claude` 可执行文件路径；该字段不得触发 provider 调用，也不得包含密钥。
- `provider_health.controls[]` 的 provider controls 必须同时保留 `kind=set_provider` 普通切换、`kind=guarded_set_provider` 预检切换和 `kind=setup_provider` 修复入口；三者都必须使用 `safety=explicit_user`，guarded command 必须追加 `--require-ready`，setup_provider command 必须来自 provider setup allowlist，当前 provider 的两类 switch 必须 disabled 并给出 `already current provider` blocker。
- `agentdeck controls` 是独立只读命令面板入口；它必须从同一次 workbench snapshot 派生 `control_registry_card`，支持 `--scope`、`--card`、`--query`、`--control-id` 和 `--enabled-only` 只读过滤参数，并复用 leader/provider/policy/agent_ready/terminal_session/role/runtime/inbox/operator controls 的 scope/card/kind/label/command/safety/enabled/blocker/agent_id/control_id，其中 provider scope 必须来自 `provider_health.controls[]` 并保留 `kind=set_provider`、`kind=guarded_set_provider` 和 `kind=setup_provider` items，guarded provider item 的 command 必须带 `--require-ready`，setup_provider item 的 command 必须来自 provider setup allowlist；policy scope 必须来自 `control_mode_card.active_controls[]`，agent_ready scope 必须来自 `agent_ready_card.controls[]`，terminal_session scope 必须同时来自 `terminal_session_card.controls[]` 和 `terminal_session_card.terminals[].controls[]`，并保留 `kind=attach_session` / `kind=open_controls` / `kind=refresh_runtime` / `kind=select_pane` items，role scope 必须来自 `role_card.agents[].controls[]` 并保留 `kind=assign_role` items，inbox scope 必须来自可见 `inbox_card.items[].controls[]` 和固定 `leader_inbox_card.items[].controls[]` 并保留 `kind=preview` / `kind=ack` items，operator scope 的批量审批派发入口必须保留 `kind=dispatch_ready`，等待回复入口必须保留 `kind=capture_reply`；自然语言审批派发局部 registry 必须保留 `scope=dispatch_preview` 的 `kind=dispatch` item 和 `scope=dispatch_batch_preview` 的 `kind=dispatch_ready` item，二者都必须使用 `safety=explicit_runtime` 且命令分别匹配 `agentdeck approval dispatch --approval-id ...` / `agentdeck approval dispatch-ready --confirm`；`source_command=agentdeck workbench`、`default_command=agentdeck controls`，并提供带 `active_filter_keys` 的 `filters` 元数据、从 `filters.control_id` 和 items 派生的 `selection`、以及从过滤后 items 派生的 `group_count` / `groups[]`；`active_filter_keys` 必须只描述当前只读投影，不得成为授权来源；无过滤时 `item_count_before_filter` 必须等于 `item_count`，只有过滤后投影才允许 before-filter count 大于当前 item_count；`selection.blocker` 未请求和命中时必须为 `null`，源 registry 不存在 control_id 时必须是 `control_id not found`，存在但被当前过滤条件排除时必须是 `control_id filtered out`；`selection.next_command` 只有在 selected control 为 enabled 时才等于该 item command，否则必须为 `null`；filters/selection/groups 不得成为第二套状态源，不得写 state、创建 chat turn、调用 provider、读取 pane 或执行任何 control/ack；输出前必须通过 `validate_control_registry_card_contract()` 自校验；`agentdeck contract controls` 必须公开 `control_registry_card_fields`、`control_registry_filter_fields`、`control_registry_selection_fields`、`control_registry_item_fields` 和 `control_registry_group_fields`，并在 `agentdeck contract list` 中可发现。
- `agentdeck controls` 中 provider scope 必须保留 `kind=guarded_set_provider` 和 `kind=setup_provider` items，并拒绝缺少 `--require-ready` 的 guarded provider command 或不在 setup allowlist 中的 setup_provider command；GUI 应优先用 kind 区分普通切换、预检切换和 setup 修复，而不是解析 label 文案。
- `agentdeck status.recovery.status=inbox_pending` 时，`next_command` 和 `recommended_action.command` 必须指向具体 `agentdeck inbox --agent <id>`，供 GUI/continue 直接打开对应 mailbox。
- `agentdeck status.recovery.status=provider_setup_required` 时，`next_command` 和 `recommended_action.command` 必须指向 `agentdeck doctor`，`recommended_action.source` 必须是 `provider_health`；该状态只用于 setup/diagnostics，不得创建 plan、chat turn、approval、message、job、inbox 或发送 tmux 输入。
- `agentdeck doctor` 必须返回当前配置 Leader 的 `configured_leader` readiness 摘要，并让顶层 `ok` 受配置 provider readiness 影响；顶层还必须返回 `deepseek`、`openai_compatible`、`codex_cli` 和 `claude_cli` provider checks，字段为 ok/detail/provider_backend/provider_transport/command_path/setup_commands，供 GUI 渲染 provider 选择诊断；这些检查不能调用 provider，不能暴露密钥值。
- `agentdeck status` 的 `inbox.heads` 是 mailbox head-only 语义的只读入口；显示或 ack inbox 前优先读取它。
- `agentdeck leader chat --message <text>` 是当前自然语言入口 MVP：它读取 ProjectView 前必须通过 `validate_project_view_contract()` 守门；无 plan 时创建 plan-only 记录、持久化一条 safe `create_approvals` Leader action，并在响应前重新读取 ProjectView，使同次响应包含刚创建的 plan、chat turn 和 action queue；有 plan 时 review 最新 plan，并持久化或复用一条 `leader_actions[]` 建议；chat 输出必须包含顶层 `leader_actions`，且它必须等于同次响应的 `project_view.leader_actions`；chat 输出还必须包含 `leader_explanation`，说明 mode、summary、reason、next_command、recommended_action_id、action_kind、action_status、safety 和 requires_explicit_user；plan/review 输出必须包含 `recovery`，且 `next_command` 必须来自 `status.recovery.next_command`；当响应包含顶层 `leader_action` 时，必须同时包含从同一 action detail 派生的 `leader_action_card`，保留 action_id/kind/status/reason/preview_command/can_apply/apply_command/explicit_command/apply_blocker/controls，且不得成为第二套 action 状态源；每轮会写入 `chat_turns[]`，可用 `agentdeck leader chat-history` 查看。
- `agentdeck leader chat --message "继续"`、`"继续吧"` 或 `"/continue"` 必须走 recovery-first 的 `mode=continue`，复用 `agentdeck continue` 的下一步卡片；顶层 `next_command`、`leader_explanation.next_command` 和 chat turn 记录必须对齐 `continue_card.next_command`；当 recovery 指向 pending inbox 时必须同时嵌入对应 agent 的 `inbox_card` 和过滤到 `scope=inbox` / `card=inbox_card` 的 `control_registry_card`，并在 `intent_card.secondary_embedded_cards` 列出这两张 companion cards；当 recovery 指向 approval queue 时必须同时嵌入 `approval_card`，当 recovery 指向 stale runtime 时必须同时嵌入同源 `runtime_card` 和 `terminal_session_card`，并在 `intent_card.secondary_embedded_cards` 列出这两张 companion cards；当 recovery 指向 `reply_waiting` 时必须同时嵌入同源 `trace_card` 且 `intent_card.embedded_card` 必须优先指向 `trace_card`，对应 inspect control 必须指向 `agentdeck trace --id <trace_card.query_id>`，否则 `validate_leader_chat_contract()` 必须拒绝响应；多条 approved approvals 时 `continue_card.next_command` 必须指向显式 `agentdeck approval dispatch-ready --confirm`；只记录 chat turn，不得创建新的 leader action、apply action、ack、approve、dispatch、capture reply、attach/select-pane 或发送 tmux 输入；`agentdeck contract leader-chat` 必须公开 `continue_card_fields`，example 必须包含稳定 `continue_card`；嵌入的 `continue_card` 必须通过 `validate_continue_contract()` 校验，嵌入的队列卡片必须复用对应 queue validator，嵌入的 `trace_card` 必须复用 `validate_trace_contract()`。
- `agentdeck leader chat --message "检查 Leader provider 配置"`、`"doctor"` 或类似 setup/diagnostics 意图必须走只读 `mode=setup`，返回 `provider_health`、`recovery`、`next_command=agentdeck doctor` 和 `leader_explanation`；只记录 chat turn，不得调用 provider，不得创建 plan/leader action/approval/message/job/inbox，不得发送 tmux 输入；`provider_health` 必须复用 workbench provider health 字段，并公开 `doctor_contract` 和 `controls[]` 供 GUI 发现 doctor schema 与 provider switch 命令；`setup_commands` 只能包含 placeholder export 命令，且不得暴露密钥值。
- `agentdeck leader chat` capability help 必须包含 `provider_switch` 能力，命令模板为 `agentdeck leader set-provider --provider <provider> --model <model>`，`safety=explicit_user`，模板 control 必须 disabled 并使用 `<provider>` / `<model>` placeholder 白名单；help mode 不得执行 provider switch。
- `agentdeck leader chat --message "查看 planner 输出"` 这类 pane capture 意图必须走只读 `mode=capture`，返回 `capture_card`，字段必须匹配 `LEADER_CHAT_CAPTURE_CARD_FIELDS` / `capture_card_fields`，并且必须附带过滤到 capture card 的 `control_registry_card`；它只记录 chat turn 和读取目标 pane 输出，不得创建 plan/leader action，不得执行 ack、dispatch、capture reply 或发送 tmux 输入；未 spawn 的 agent 必须返回错误且不创建 chat turn 或 plan。`"捕获 planner 对 msg_xxx 的回复"` 这类 capture-reply 意图也走 `mode=capture`，但只嵌入 `trace_card` 和建议显式 `agentdeck capture-reply --agent <id> --message-id <msg_id>`，不得读取 pane 或写 reply；`"捕获当前回复"` 只能在 latest review 是 `wait_for_reply` 时从 review 解析目标。
- `agentdeck leader chat --message "查看 planner inbox"` / `"查看 leader inbox"` 这类 inbox 意图必须走只读 `mode=inbox`，复用 `agentdeck inbox --agent <id>` 的 queue shape 返回 `inbox_card`；`<id>` 可以是配置里的 worker agent，也可以是逻辑 Leader mailbox owner `leader`，用于查看 worker reply 回流，但不代表 Leader 有 tmux pane；`"查看当前 inbox"` / `"确认当前 inbox"` 可在 recovery 指向 pending inbox 时从 `target_id` 反查 mailbox owner，recovery 不是 inbox 时不得猜测目标 agent；包含 `追踪`、`trace` 或 `lineage` 且存在 pending head 时，`next_command` 可推荐该 head 的 `agentdeck trace --id <inbox_id>`，并在能解析 lineage 时嵌入同源 `trace_card`，让 `intent_card.embedded_card=trace_card` 直接展示通信证据链；包含 `确认`、`ack` 或 `acknowledge` 且 head 可 ack 时，`next_command` 可推荐该 head 的 `ack_command`，但 `leader_explanation.safety` 必须是 `explicit_runtime` 且 `requires_explicit_user=true`；该模式只记录 chat turn，不得创建 plan/leader action，不得执行 ack、dispatch、capture reply 或发送 tmux 输入；嵌入的 `inbox_card` 必须通过 `validate_inbox_contract()` 校验，同一响应必须附带过滤到 `scope=inbox` / `card=inbox_card` 的 `control_registry_card`；普通查看可以只展示 preview/ack controls，`inbox_trace` 和 `inbox_ack` 必须让 registry selection 选中与顶层 `next_command` 相同的 preview 或 ack control；嵌入的 `trace_card` 必须通过 `validate_trace_contract()` 校验。
- `agentdeck leader chat --message "追踪 msg_xxx"` / `"追踪 art_xxx"` 这类 direct trace 意图必须走只读 `mode=trace`，复用 `agentdeck trace --id <id>` 的 response shape 返回 `trace_card`；该模式只记录 chat turn，不得创建 plan/leader action，不得执行 ack、dispatch、capture reply 或发送 tmux 输入；嵌入的 `trace_card` 必须通过 `validate_trace_contract()` 校验，未知 trace id 必须返回错误且不创建 chat turn 或 plan。
- `agentdeck leader chat --message "查看审批"` 这类 approval 意图必须走只读 `mode=approval`，复用 `agentdeck approval list` 的 queue shape 返回 `approval_card`；包含 `批准` 或 `approve` 且存在 pending approval 时，`next_command` 可推荐第一条 pending approval 的 `approve_command`；包含 `拒绝`、`驳回` 或 `reject` 且存在 pending approval 时，`next_command` 可推荐第一条 pending approval 的 `reject_command`；包含 `派发` 或 `dispatch` 且存在 approved approval 时，`next_command` 可推荐第一条 approved approval 的 `dispatch_command`，并嵌入 `dispatch_preview_card` 作为 GUI-ready 执行前确认卡；当 `dispatch_preview_card.blocker` 非空时，card 内 dispatch control 与 intent next control 必须 disabled 且 blocker 相同；包含 `所有`、`全部`、`all` 或 `batch` 的批量派发意图必须返回 `dispatch_batch_preview_card`，items 复用单条 dispatch preview 字段和 controls，顶层 `next_command` 必须指向 `agentdeck approval dispatch-ready --confirm`，但 chat 不得自动派发；approve/reject/dispatch 建议的 `leader_explanation.safety` 必须是 `explicit_runtime` 且 `requires_explicit_user=true`；该模式只记录 chat turn，不得创建 plan/leader action，不得执行 approve/reject/dispatch-ready/dispatch，不得创建 message/job/inbox 或发送 tmux 输入；嵌入的 `approval_card` 必须通过 `validate_approval_contract()` 校验，嵌入的 `dispatch_preview_card` 必须匹配 `LEADER_CHAT_DISPATCH_PREVIEW_CARD_FIELDS` / `dispatch_preview_card_fields`，嵌入的 `dispatch_batch_preview_card` 必须匹配 `LEADER_CHAT_DISPATCH_BATCH_PREVIEW_CARD_FIELDS` / `dispatch_batch_preview_card_fields`。
- `agentdeck leader chat --message "apply action <id>"` 只能复用 safe apply-action 白名单；不得通过 chat 自动应用 dispatch/capture 类 action；safe apply 完成后的 `next_command` 必须来自刷新后 `recovery.next_command`，例如进入 `agentdeck approval list`，并且当 safe apply 创建 approvals 时必须嵌入同源 `approval_card` 供 GUI/对话层展示审批队列，但不得自动 approve/reject/dispatch 或发送 tmux 输入。
- `agentdeck leader plan` 和 `agentdeck leader chat` 默认读取 `.agentdeck/config.toml` 的 `[leader] provider/model`；需要本地 dry-run 时必须显式使用 `--provider fake --model fake-plan`。
- 保存后的 plan、ProjectView `plans.items[]`、`agentdeck plan list` 和 run cards 必须暴露 `provider_backend`（`local`、`api`、`cli`、`unknown`）和 `provider_transport`（`local`、`http`、`subprocess`、`unknown`），并暴露同源 `leader_backend` 对象：`agent_id=leader`、provider/model、backend/transport、`reasoning_backend`、`runtime_kind=logical_leader`、`pane_backed=false`、`pane_id=null`、`approval_required=true`、`dispatch_ready=false`；这些字段只表示 plan provenance，方便 GUI/审计区分 fake/API-backed/CLI-backed Leader 来源和调用通道，不得作为授权、dispatch 或 runtime 执行依据。
- `agentdeck run --task <text>` 是 approval-gated run loop 启动入口：它可以调用 Leader provider 生成 plan 并立即创建 pending approvals，但必须停在 `agentdeck approval list` / 显式 approve controls。`agentdeck run --plan-id <id>` 只能返回只读 `run_progress` card，聚合 plan status、Leader review 和 run-specific approval queue；不得自动 approve、dispatch、capture pane、ack inbox、写 state 或发送 tmux 输入，输出前必须通过 `validate_run_start_contract()` 自校验。
- `agentdeck run-loop --plan-id <id> --confirm` 是只读 `agentdeck loop` 的 write 对应命令，也是有界自主模式的执行引擎：它必须要求 `--confirm` **且** `config.leader.approval_mode == "autonomous"`，缺任一都要拒绝且不写任何 state；未知 plan 也必须拒绝且不写 state。它只组合两个已被单独 sanction 的动作，scope 到单个 plan：复用 `select_auto_approvals` 自动批准该 plan 内 allowlist、预算内的 pending approvals（逐项 `store.decide_approval(..., reason="autonomous")` + `approval_decided` with `source=autonomous`），复用 `_approval_dispatch_preview_card` / `_dispatch_approved_approval` 把 approved-and-ready 的审批派发给已有 running pane 的 agent（`approval_dispatched`），再复用 `store.leader_review(plan_id)` + 纯 `run_loop_gate(review, has_error, plan_id)` 诊断当前人类 gate 并停下。它绝不 force-spawn（目标 agent 没有 running pane 的审批记为 `blocked`，交回 `agentdeck agent spawn --agent <id>`），绝不从 pane 捕获回复、绝不推断 worker 完成；唯一接受的完成信号是 worker 自己写出的文件通道回复（2026-07-25 human 批准），且摄入与 gate 解耦：每 wave 计算本 plan 的 awaiting 集（已 dispatched 且 message 无 reply 的审批），对其中 `.agentdeck/replies/<message_id>.reply.txt` 已存在结构化回复者逐个复用文件通道摄入路径（`record_reply` + `reply_captured` `captured_from=file`/`pane_id=null` + 每条一条 `run_loop_reply_captured` 汇总事件）回收，再重算 gate 一次；当前 `stopped_reason`（含 blocked/needs_human_approval）不得遮蔽摄入。结果记入可选 `captured_replies[]`；摄入发生在派发之前，使同 wave 内"前一 step 文件就绪→摄入→解锁下一 step 派发"成立。派发遵守**step 顺序守卫**：每 wave 只派发最早未完成 step（完成 = rejected 或 dispatched 且已有 reply）的 approved 审批，更晚 step 保持 approved 并记入 `skipped[]`（reason `awaiting earlier step completion`）；当 wave 持留了后续 step、无真实 blocked 且最早 step 仍在等回复时，gate 以该 step 的 `waiting_for_reply` + 显式 capture 命令呈现，不得推荐派发被持留审批。文件不存在的 awaiting 项行为不变——最终停在对应 gate（`waiting_for_reply` 时携带可选 `reply_file_ready`），交回显式 `agentdeck capture-reply --agent <id> --message-id <id>`。摄入以 awaiting 集为界，绝不读 pane 输出。`run-loop --all` 的每计划循环应用同一 step 顺序守卫（held 项记入该计划 `skipped[]`），并在派发前做与单计划 wave 同源的文件摄入（共享 `_ingest_plan_reply_files`，以该计划 awaiting 集为界，绝不读 pane，结果记入该计划可选 `captured_replies[]`）。一次调用只走一轮 auto-approve+dispatch（+至多一次文件摄入）wave 再停在 gate；显式 `--follow --max-waves <n> --interval <s>` 是有界多 wave 模式（仅支持 `--plan-id`，`--all --follow` 拒绝，`--max-waves < 1` 拒绝）：前台重复同一单 wave 引擎直到 gate 不再是 `waiting_for_reply` 或达到上限（例外:本 wave 追加了 review 迭代轮时继续,见 review 迭代闭环规则），输出 `mode=run_loop_follow`（`waves[]` 每项为完整单 wave payload + `wave` 序号并逐项过单 wave validator），完成追加一次 `run_loop_follow_completed`，输出前必须过 `validate_run_loop_follow_contract()`；可选 `--release-boxes` 在段首（wave 0，两段 follow 之间弹出的框否则整段错过）和每个 wave 间隙各跑一次委托扫描，只释放命中活跃委托前缀的授权框（复用 `_scan_release_delegated_boxes`，每次释放审计 `auth_box_released` `source=run_loop_follow`，未命中框绝不代按，见 delegation contract）；可选 `--merge-on-complete` 仅在最终 gate 为 `complete` 时复用 `worktree merge-plan` 核心按 step 顺序合并该 plan 的任务分支（结果记入可选 `plan_merge`，冲突即 abort 并如实报告，其它 gate 绝不合并）；`agentdeck worktree merge-plan --plan-id <id> --confirm` 是对应的显式整 plan 合并命令，必须要求 `--confirm` 且 plan gate 为 `complete`，abandoned/缺分支/已合并项进 `skipped[]`，冲突 abort 记 `failed` 并非 0。`stopped_reason` 取值为 `error`/`blocked`/`needs_human_approval`/`waiting_for_reply`/`complete`/`idle`，每个都对应一条显式 `next_command`。它追加一次 `run_loop_advanced` 汇总事件（可在 `agentdeck history` 查看，humanize 为 `Run-loop advanced`），输出前必须通过 `validate_run_loop_contract()` 自校验，失败时返回非 0、不打印半坏 JSON 并追加 `run_loop_contract_failed`；`agentdeck contract run-loop` 是其发现入口。它绝不调用 Leader provider、不创建新 plan、不 ack inbox、不修改 approval/runtime 之外的 state，也不发送 tmux 输入之外的任何 runtime 操作。
- `agentdeck run-loop --all --confirm` 是并行调度器，也是第一个写操作多计划命令：把单计划 wave 扩展成对**所有 active 计划跑一轮 round-robin**。它和 `run-loop --plan-id` 共用 `--confirm` + `config.leader.approval_mode == "autonomous"` 安全门，且必须恰好二选一（`--plan-id` 与 `--all` 互斥，缺任一或同时给都要拒绝且不写 state）。单计划 `run-loop --plan-id` 行为必须保持不变——调度器是 `_run_loop_all` 独立新增代码，只复用底层原语（`select_auto_approvals`、`_approval_dispatch_preview_card`、`_dispatch_approved_approval`、`run_loop_gate`、`store.leader_review`），不得改写单计划路径。它按创建顺序遍历 gate `!= complete` 的 active plans，每个计划跑一遍 wave；预算是**整波共享**的 `[autonomous].max_approvals`（总 auto-approve ≤ 预算，`budget = {max_approvals, used, remaining}` 且 `used+remaining==max_approvals`）；agent 冲突按**跳过**处理——`_busy_agents(store)` 收集"已派发未回收"的 agent，本波不再派给它，命中的审批记入该计划 `skipped_contention[]`（保持 approved、blocker=`agent busy this wave`），busy 集合在派发时随之增长；一波就停。输出 `mode=run_loop_all`、`requires_explicit_user=true`、`safety=delegated`、`plan_count`/`active_count`(==len(plans))/`budget`/`totals`/`plans[]`，每个 plan item 复用 run-loop 的 dispatched/blocked/skipped 形状并加 `skipped_contention` 和 `gate`(run-loop gate 枚举)/`next_command`，输出前必须通过 `validate_run_loop_all_contract()` 自校验；追加一次 `run_loop_all_advanced` 汇总事件（`agentdeck history` 渲染为 `Parallel wave`）。安全边界与 `run-loop` 完全一致:只 approval-gated、只白名单+共享预算、只派 running 且不忙的 pane、绝不 force-spawn、绝不 capture reply、绝不推断完成。`agentdeck contract run-loop-all` 是其发现入口。
- `agentdeck run-loop-host start|status|stop` 是 run-loop 背景宿主：把**未改动**的单 wave 引擎(`_run_loop_single_wave` 逐字节复用)放进 detached 子进程(`start_new_session=True`,stdio DEVNULL),使确认后的自主段在客户端断开后继续。`start` 必须同时满足四道 gate——`--confirm`、`config.leader.approval_mode == "autonomous"`(与 run-loop 同门)、显式 `--max-waves >= 1`(强制有界预算,无 unbounded host)、已知 `--plan-id`——任一不满足拒绝且零写零 spawn;活 pid 单例互斥(第二次 start 拒绝,stale 记录不挡),成功写 `.agentdeck/run-loop-host/host.json` 单例记录(原子替换,损坏读作无宿主)+ `run_loop_host_started` 审计事件,输出经 `validate_run_loop_host_start_contract()` 守门。serve 子进程每 wave 前重读 `.agentdeck/config.toml`:`approval_mode` 离开 autonomous 即在下一 wave 边界自停(`stopped_reason=policy_revoked` 远程刹车);wave payload 逐条追加 `.agentdeck/run-loop-host/host.log`(append-only JSONL,绝不截断重写),gate 不再 `waiting_for_reply` → `gate_reached`,预算用尽 → `budget_exhausted`,引擎异常/契约失败 → `engine_error`(日志只记异常类型,绝不记 provider 输出);SIGTERM/SIGINT 只置旗标,当前 wave 完整跑完后以 `signalled` 停——worker 绝不被半途切断;可选 `--release-boxes` 在段首和 wave 间隙复用 `_scan_release_delegated_boxes`(source=run_loop_host,逐次审计,未命中框绝不代按),可选 `--merge-on-complete` 仅在最终 gate 为 complete 时先过 `_verdict_merge_blocker` 再复用 `_merge_plan_worktrees`。`status` 纯只读(无记录/running/stale/干净停止四态,running 与 stale 互斥),经 status validator 守门,不写 state、不碰 tmux;`stop --confirm` 对活 pid 只发一次 SIGTERM 并有界等待,超时**绝不升级 SIGKILL**(`run_loop_host_stop_timed_out` 退非 0,记录保留给人工),stale 记录清 pid 不发信号(`run_loop_host_stale_cleared`),干净停止追加 `run_loop_host_stopped`(source=explicit)。`stopped_reason` 闭合枚举 `gate_reached/budget_exhausted/policy_revoked/signalled/engine_error` 单一来源在 `src/agentdeck/run_loop_host.py`。hosting 不是授权:wave 引擎全部不变量原样继承(approval-gated、白名单+预算、只派 running pane、绝不 force-spawn、绝不读 pane 推断完成、只认文件通道回复、step 顺序守卫);本宿主与 M2 Mission daemon(`agentdeck daemon …`、`src/agentdeck/daemon/`)刻意分离,后者不受触碰;不做多计划宿主、无界宿主、crash 自动重启、remote 宿主。契约维护在 `docs/contracts/run-loop-host-schema.md`,发现入口 `agentdeck contract run-loop-host`;修改三响应字段、gate、stopped_reason 枚举、安全边界或 validator 时必须同步该 contract、contract index、README、HISTORY 和测试。
- Review 迭代闭环(review iteration loop)把 review verdict 变成有界的自动回炉:纯模块 `src/agentdeck/review_iteration.py` 只做推导(零 IO、零 LLM、不 import cli/state),`StateStore.append_review_iteration(plan_id, max_review_rounds, source)` 是**唯一** state 写点(已注册进 `AUTHORITATIVE_STATE_MUTATION_METHODS`,整段 load→derive→mutate→save 在 `_protocol_mutation_lock` 内),两个消费面(run-loop 引擎钩子、`agentdeck plan rework --plan-id <id> --confirm`)都只调用它。触发条件闭合且 fail-closed:该 plan **最新**有效 verdict(与 `plan_verdict_summary` 同源选取)且 `overall ∈ {fail, needs_changes}` 才触发;**rework 自评排除(2026-08-01 终审接缝修复)**——追加 step 带 `iteration_kind`(rework|review),rework step 的派发 prompt 不注入验收标准/verdict 输出指令,且 rework 回复里的自评 `verdict:` 经同源 `rework_step_numbers` 双面排除:既不触发迭代(防"coder 复审自己"的幻影轮烧预算)也不进入 `plan_verdict_summary`(防自评 pass 越过 reviewer 放行 merge、自评 fail 污染判定);criterion 级 unknown、无 `verdict:` 行、无效 verdict 一律不触发;同一 reply 幂等(追加 step 记 `triggered_by_reply`,消费过的 reply 绝不重复触发);拒绝原因是闭合六值枚举 `no_plan/no_verdict/verdict_pass/already_triggered/rounds_exhausted/no_implementation_step`,任何拒绝路径零写。一轮迭代=向同一 plan 追加 rework + re-review 两个 step(编号顺延,带 `origin=review_iteration`/`round`/`triggered_by_reply` provenance):rework 的 agent/任务源取被审实现 step(worktree-branch 归属优先、已派发回退,round-2 起瞄准上一轮 rework step),任务文本是**确定性模板**(fail 标准原文+证据+reviewer 回复原文,4000 字符截断附 `agentdeck trace --id <rep_id>` 指引,结尾固定 commit 指令,绝不调用 provider、不读产物文件);re-review 逐字节复用原 review step 的 agent/role/任务。追加的 step 生成**普通 pending 审批**,由既有 autonomous 白名单+`max_approvals` 预算与 step 顺序守卫接手——零新授权面。预算:`[autonomous] max_review_rounds`(缺省 2,fail-closed 解析,`policy set-mode` 重写配置时保留既有值)+ `run-loop`/`run-loop-host start` 的 `--max-review-rounds N` 覆盖(负数拒绝零写);`0` = 关闭迭代且行为逐字节同无此功能;计数按 plan 生命周期从 step round 标记推导;预算耗尽不追加、gate 回到 complete + `verdict_blocked` 扣 merge,显式 `plan rework` 同样受预算约束,越过预算只能人工改配置,绝不静默。引擎钩子在单 wave 摄入之后、派发之前(auto-approve 选取已过,追加审批本 wave 保持 pending、下一 wave 接手);wave payload 可选 `review_iterations[]` 只在追加成功或 `rounds_exhausted` 时出现,其余拒绝静默(payload 逐字节稳定);**walk-away 链路例外(2026-07-31)**:`--follow` 与 run-loop-host serve 的 gate 退出条件在"本 wave 追加了迭代轮(`review_iterations` 含 `round` 项)"时继续跑下一 wave(仍受 `--max-waves` 硬界,gate 诚实性不变——wave 自身仍如实报 `needs_human_approval`),使 fail → 追加 → 下轮批准派发 rework → 复审 → pass → complete → merge 在一次调用内闭合;`run-loop --all` 对 gate0=complete 的 plan 先跑钩子再决定跳过(pre-gate 例外,见 run-loop-all contract:该分支追加的审批可在同 wave 经同一 allowlist/预算选取与派发路径处理,安全不变量不弱化),拒绝时跳过行为逐字节不变。`plan rework` 无 `--confirm`/未知 plan/无触发条件/预算耗尽一律拒绝零写非 0,成功输出经 `validate_plan_rework_contract()` 守门,不派发、不 auto-approve、不读 tmux;**无 `--refine` 时也不调 provider**(带 `--refine` 才在锁外调用一次,见下);契约维护在 `docs/contracts/plan-rework-schema.md`,发现入口 `agentdeck contract plan-rework`(skip_reasons/trigger_overalls 单一来源自纯模块)。审计:每次追加一条 `plan_rework_appended`(plan_id/round/source/triggered_by_reply/steps/approval_count);ProjectView `plans.items[].review_rounds` 与 plan_status steps 的 origin/round/triggered_by_reply 只是 provenance,不是权限或执行授权。merge gate:最新 verdict 非 pass 时 `merge-on-complete` 扣住,迭代成功后最新 pass 自然放行;**零 verdict 收紧(2026-08-01 user 拍板)**——派发过 review step(注入过验收标准、期待判定,信号与 G5 注入同源:message 带非空 `worktree_base_branch` 且该步非 rework)却一份有效 verdict 都没有时,自动合并同样扣住(`no review verdict recorded…`);从未要求判定的 plan(无 worktree/纯实现 plan)行为不变;人类 `worktree merge-plan --confirm` 永不受 gate;G5"gate complete + 只扣 merge"语义是本闭环 `--max-review-rounds 0` 切片。**Leader 精修回炉任务(2026-08-01 user 拍板,显式命令二期)**:只有 `agentdeck plan rework --plan-id <id> --confirm --refine` 会调用配置的 `[leader]` provider 把审查意见提炼成一份聚焦的回炉任务文本;`--refine` 必须与 `--confirm` 同给(单给拒绝、零写、且在拒绝前绝不构造 provider),触发判定先在**锁外**只读推导一次(不满足条件照旧拒绝且零 provider 调用),provider **只调一次不重试**且全程在 `_protocol_mutation_lock` **之外**(锁内绝不做秒级 I/O),locked writer 锁内重新推导(唯一权威)并仅在 `triggered_by_reply` 与 `override_for_reply` 相等时采用,漂移则回落模板并记 `refine_skipped_reason="state_changed"`;**run-loop / run-loop --all / --follow / run-loop-host 永不精修**——代码层面不提供该参数入口("run-loop 绝不调用 Leader provider"这条 live 验证的不变量完整保留,walk-away 失败面不增加);精修产出只进 rework step 的 `task`(agent/role/编号/provenance 与模板路径逐字节一致,固定 commit 收尾指令由程序追加、绝不依赖模型自觉,复审步任务仍逐字节复用原 review step),落地时 step 记 `task_source=leader_refined`(模板路径省略该键,缺省即 template),该字段只是 provenance,不是权限或执行授权;**provider 失败一律回落确定性模板且命令仍退出 0**(迭代不因 provider 抖动阻断,模板版本身就是可用产物),响应记 `refined: false` + 闭合枚举 `refine_skipped_reason ∈ {provider_error, invalid_output, state_changed, unsupported_provider}`,provider 异常另记 `leader_errors[]` + `leader_provider_failed`(`mode=plan_rework_refine`),只留枚举码、绝不留存 provider 原文;响应 `refined`(bool)与可选 `refine_skipped_reason` 经 `validate_plan_rework_contract()` 守门。非目标:run-loop 自主段使用精修、精修复审任务、多轮精修、让模型改动 step 结构、跨 plan 迭代。修改触发条件、模板、精修接线、预算、`review_iterations[]`、skip_reasons 或 validator 时必须同步 plan-rework/run-loop/run-loop-all/project-view 契约、contract index、README、HISTORY 和测试。
- Review 组 + round_reviewer(2026-08-01)把"一个 review 环节"从单人扩成可配置的确定性 reviewer 组:可选 `[review]` 配置节只有两个键——`reviewers = [<agent_id>, ...]`(有序 reviewer 组,空列表=关闭)和 `round_reviewer = <agent_id>`(迭代复审换人);两者都 fail-closed 解析(未知 agent、空串、重复成员、非列表、类型错一律 `ValueError`,零静默降级),**缺省无 `[review]` 节时所有路径逐字节不变(唯一例外:`verdict_summary` 恒带附加的 `group` 投影,单 reviewer 为 `size=1`)**。纯模块 `src/agentdeck/review_group.py` 承担全部推导(零 IO、零 LLM、不 import cli/state/config,reviewers 以 `(agent_id, role)` 纯数据传入):`expand_review_group(plan, reviewers)` 在 `cli._generate_leader_plan` 的**两个** return 点(单阶段与 G2 split)把每个 review step 确定性展开为 N 个连续 step,识别谓词是 **step 的 role 等于 `reviewers[0]` 的角色**——首位 reviewer 是主 reviewer,其角色签名定义什么算 review 环节(跨角色组时不会误伤 planning step);展开后 step 编号顺延重排,每个成员带 `origin="review_group"`/`review_group`(组号)/`review_group_member`(组内序号),输入 plan 永不被修改,`record_plan` 零改动。执行引擎零改动:组成员仍是线性 plan 里的普通串行 step,由既有 step 顺序守卫逐个派发,**绝不并行派发同一 review 环节**。聚合是 **any-fail-blocks**(`REVIEW_GROUP_RULE = "any_fail_blocks"`,单一来源):overall 取组内最严(fail > needs_changes > pass),criteria 逐条合并(任一 fail 即 fail,否则任一 unknown 即 unknown,全 pass 才 pass),score 取最小。选取是**组完成才判定**:`latest_complete_group()` 只认最新一个每个成员都已回复带 verdict 的组,组未齐时**触发面**(`derive_review_iteration`)一律拒绝 `no_verdict`,绝不让先 fail 的成员开一轮、后审旧代码的成员再开一轮双烧预算;而**展示与 merge gate 面**(`plan_verdict_summary`,以 `select_plan_verdict(..., require_complete_group=False)` 走 `latest_group_status()`)必须对已报到成员聚合并标 `group.complete=false`——组内一人 verdict 无效/缺失时另一人的有效 fail 绝不能随整组消失,否则启用多 reviewer 反而放开了自动合并(2026-08-01 终审 Critical);`_verdict_merge_blocker` 在 `complete=false` 时一律扣住自动合并(未报到成员以 `overall=null` 占位),人类 `worktree merge-plan --confirm` 永不受 gate;一份 verdict 都没有时仍返回 `null`(维持既有语义)。幂等是**组级**的:追加 step 记的 `triggered_by_reply` 是该组**最后一个成员**的 reply id,同一组绝不重复触发。`plan_verdict_summary` 与 `derive_review_iteration` 共用这套组感知选取(单一来源),rework 自评在组路径上是**结构性**排除的(rework 成员从不带 `review_group`,故永不进组;非组路径才走 `rework_step_numbers`)。迭代追加的复审步同样是**组感知**的:配了 `reviewers` 时追加的是整个复审组(带 `origin="review_iteration"` + 同一套组标记),只配 `round_reviewer` 时单条复审步换成该 agent(换人不换语义:仍是普通 pending 审批、仍受同一预算与顺序守卫)。`verdict_summary.group`(`size`/`complete`/`rule`/`members[]`)是三面(leader review / leader summary / run_progress)共有的只读 provenance,单 reviewer 路径投影隐式 `size=1` 组让 GUI 单双路径同构;`plan status` step 的 `review_group`/`review_group_member` 同样只是 provenance——它们都不是权限,不授权 dispatch、不改 gate、不绕过审批。非目标:同一 review 环节的并行派发、majority/加权聚合、跨组仲裁、Leader 自选 reviewer、组未齐提前触发。修改 `[review]` 解析、展开谓词、聚合规则、组完成语义或 validator 时必须同步 leader-review/leader-summary/run/project-view 契约、CLAUDE.md、README、HISTORY 和测试。
- `agentdeck leader set-provider --provider <provider> --model <model>` 是持久切换默认 Leader provider 的显式命令；它必须回显目标 backend 的 `ready`、`supported`、`missing_env`、`detail`、`command_path` 和 `setup_commands`，只修改 `.agentdeck/config.toml` 的 `[leader] provider/model` 并追加 `leader_provider_updated` 事件，不调用 provider、不创建 plan/action/approval/message/job/inbox、不发送 tmux 输入；未知 provider 必须失败且不得修改配置；加 `--require-ready` 时，目标 provider 不 ready 必须拒绝写配置、追加 `leader_provider_update_rejected` 事件并返回非 0。
- 真实 Leader API 可以使用 `agentdeck leader plan/chat --provider deepseek`，环境变量为 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL` 和 `DEEPSEEK_MODEL`；也可以使用 `--provider openai-compatible`，环境变量为 `AGENTDECK_LEADER_API_KEY`、`AGENTDECK_LEADER_BASE_URL` 和 `AGENTDECK_LEADER_MODEL`；`--model <model>` 或配置中的 `[leader].model` 必须作为 API 请求体 `model` 优先于环境默认值；真实 provider 也只能生成 plan，不得绕过审批。
- CLI-backed Leader 可以使用 `agentdeck leader plan/chat --provider codex-cli` 或 `--provider claude-cli`，通过本地 `codex exec` / `claude --print` 为 `agent_id=leader` 生成同一 JSON plan schema；`agentdeck leader plan/chat --model <model>` 或配置中的 `[leader].model` 必须透传给本地 CLI 的 `--model` 参数，不能只作为 state 里的记录标签；stdout 可以是纯 JSON plan，也可以把唯一 JSON plan 包在 Markdown fenced `json` block 中；解析后必须复用 provider plan schema validator，要求顶层包含非空字符串 `goal`、非空字符串 `summary`、非空 `steps`，要求每个 step 包含从 1 开始连续且唯一的正整数 `step`、非空字符串 `agent_id`、`role`、`task`、`risk`、`requires_approval`，且 `agent_id` 必须指向当前配置中的 worker agent，`role` 必须匹配该 worker agent 的配置角色，强制 `approval_required=true`、`dispatch_ready=false`，并继续要求每个 step 都 `requires_approval=true`；不得复用 worker tmux pane 作为 Leader，也不得自动创建 approval、dispatch 或发送 tmux 输入。
- 所有真实 Leader provider 的 planning prompt 必须包含每个 worker 的 `agent_id`、`role`、`role_prompt`、provider 和 workspace mode，并包含当前 ProjectView `skills` 派生的 compact loaded skill context；prompt 必须明确要求 step 编号为 `1..n`、只能使用列出的 worker `agent_id`、每个 `role` 必须完全复制对应 worker role，让 Leader 按角色职责和已加载 workflow context 拆分任务；这不授权自动派发，仍只生成审批 plan。
- CLI-backed Leader readiness 只检查本地命令是否存在并提供 `codex login` / `codex doctor` 或 `claude auth` / `claude doctor` setup commands；不得要求或暴露 API key。
- 真实 provider 失败必须记录到 `leader_errors[]` 和 `leader_provider_failed` 事件；不要让异常崩溃 CLI，也不要半写入 plan。
- 自然语言任务调度优先从 `agentdeck leader plan --task <text>` 生成 plan-only 记录开始；不要跳过 plan 直接自动 dispatch。
- `agentdeck leader review --plan-id <id>` 是当前本地 Leader review loop，进入 review 前必须通过 ProjectView contract 守门，用于基于 approval/dispatch/reply 状态建议下一步；输出必须包含同源 `leader_backend`、`next_command` 和 GUI-ready `controls[]`，其中 `leader_backend` 必须保持逻辑 `agent_id=leader`、`runtime_kind=logical_leader`、`pane_backed=false`、`pane_id=null`，`wait_for_reply` 推荐 `agentdeck capture-reply --agent <id> --message-id <id>` 但不执行 capture。
- `agentdeck leader next` 进入 action queue 前必须通过 ProjectView contract 守门；它只把下一步建议写入 `leader_actions[]`，不得直接执行 action；相同 pending action 已存在时会复用原 action_id；执行仍需人类显式运行对应 approval/dispatch/capture 命令；`agentdeck leader actions` 必须暴露 `recommended_action_id`、每项 `is_recommended`、`preview_command` 和 `controls[]`。
- `agentdeck leader action --action-id <id>` 是 action 执行前的只读详情入口；输出必须包含当前 `recovery`、`recommended_action` 和 `matches_recommended_action`，apply 前必须检查 `preview_command`、`can_apply`、`explicit_command` 和 `apply_blocker`。
- `agentdeck leader apply-action --action-id <id>` 执行 safe apply 前必须通过 ProjectView contract 守门；当前只允许应用 `create_approvals`，不得自动应用 dispatch/capture 类 action。
- 审批、dispatch 或恢复任务前优先用 `agentdeck plan list`、`agentdeck plan show --plan-id <id>` 和 `agentdeck plan status --plan-id <id>` 检查计划。
- 使用 `agentdeck approval create-from-plan --plan-id <id>` 创建审批项，使用 `approval approve/reject` 更新状态；只有 approved approval 才能通过 `agentdeck approval dispatch --approval-id <id>` 派发；`agentdeck approval dispatch-ready --confirm` 是显式批量派发命令，只派发 approved 且目标 agent runtime ready 的审批项，blocked 项必须保留为 approved 并返回 blocker，不带 `--confirm` 必须失败且不得写 state 或发送 tmux 输入。
- `dispatch`、`approval dispatch`、`approval dispatch-ready --confirm`、`reply` 和 `capture-reply` 的成功 JSON 输出必须包含或逐项包含 `trace_command`，指向对应 message/reply lineage，供 GUI 和人类直接追踪；当结构化 reply 含 `full_output_path:` 时，`reply` / `capture-reply` 必须登记 artifact 摘要并在成功 JSON 中返回 `artifacts`，但不得读取文件内容；`approval dispatch` 成功响应还必须嵌入目标 agent 的同源 `inbox_card`，复用 `agentdeck inbox --agent <id>` 队列形状，但不得自动 ack；`reply` / `capture-reply` 当 reply 回流到某个 agent inbox 时也必须嵌入接收方 `inbox_card`，但不得自动 ack 或继续 review。
- Worker 输出结构化结果后，优先使用 `agentdeck capture-reply --agent <id> --message-id <id>` 从 pane 回收入账；手动 `reply` 作为兜底。
- `agentdeck trace --id <id>` 可用 message/attempt/job/reply/artifact/inbox 任意 ID 还原通信链路；artifact trace 只返回路径摘要，不读取文件内容。
- 使用 `agentdeck ack --agent <id> --inbox-id <id>` 时只能确认该 agent 最早的 pending inbox item；非 head item 必须等待前序 item ack 后再处理。
- 先更新架构/README/agent 文档，再扩展行为。
- 所有开发命令默认先激活 `agentdeck` conda 环境。
- `References/` 只读学习，不纳入 git，不直接复制大段源码。
- 不要重写终端模拟器；MVP 复用 tmux。
- 不要绕过审批执行危险操作。
- 不要把 provider、runtime、state、orchestration 逻辑混在一个文件里。
- Worker 写过文件后，Leader 汇总前必须重新读取相关文件。

## 优先级

1. 保持本地可运行。
2. 保持状态可追踪。
3. 保持人类可审批。
4. 保持 runtime 可见。
5. 再考虑 GUI、remote、MCP、skills 自动化。
